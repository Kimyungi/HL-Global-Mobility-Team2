# CAN v3 (PR #47) — 적용·롤백 이력 및 재적용 절차

## 결론: **롤백 완료. 현재 워크스페이스는 v2(main `c3f01bb`) 상태다.**

| 시각 | 일 |
|---|---|
| 2026-08-24 23:2x | PR #47 브랜치 체크아웃 + `fma_interfaces`·`bridge_dspace` 빌드 → v3 적용 |
| 2026-08-24 23:30 | **롤백** — dSPACE 가 아직 v2 라서 |

## 왜 되돌렸나

프로토콜은 양쪽 합의다. dSPACE 가 v2 인 채로 PC 만 v3 로 두면:

1. **얻는 게 0** — dSPACE v2 는 `0x203`~`0x235` 를 안 보낸다. v3 의 유일한 목적인
   51점 MPC 궤적이 안 오고 `/vehicle/mpc_trajectory` 는 무발행. 경고조차 안 뜬다.
2. **조향 무반응 위험** — PROTOCOL.md v2 는 `n_points` 가변(1~20)을 규정하므로 프레임
   해석은 안 깨진다. 그러나 CLAUDE.md §3 ② 의 실측은 **"검증된 포맷은 20점뿐, 1점은
   8/8 무반응"** 이다. 3점은 그 사이의 미검증 구간이고 실측이 가리키는 방향이 나쁜 쪽이다.
   최악은 v_ref 1.0 m/s 로 직진해 트랙 이탈.

## 검증된 것 (재적용 때 다시 안 해도 됨)

PR 작성자가 "ROS 2/colcon·SocketCAN 환경이 없어 못 했다"고 남긴 공백을 이 PC 에서 메웠다:

- `colcon build` — **C++ 컴파일 통과** (fma_interfaces 7.0s / bridge_dspace 5.5s)
- `protocol_selftest.py` — **ALL PASS** (3점 상한 / 20점→0·10·19 축약 / 51점 세트 / P00 유실 폐기 / watchdog)
- `can_bridge_node` 실기동 — 배너 `TX 0x100+0x101..0x103, RX vehicle 0x200..0x202 + trajectory 0x203..0x235`,
  `tx=0 rx=0` 으로 **자발 송신 없음** 확인
- 미검증으로 남은 것: **vcan 루프백** (sudo 필요). 재적용 시:
  `sudo modprobe vcan && sudo ip link add dev vcan0 type vcan && sudo ip link set up vcan0`
  → `ros2 launch bridge_dspace loopback_test.launch.py`
  → `/vehicle/vector` 와 `/vehicle/mpc_trajectory` 둘 다 ≈100Hz

## 롤백 완료 확인 (2026-08-24 23:30 실측)

- `fma_interfaces/msg/MpcTrajectory` — 소멸 ✅
- `protocol_selftest.py` — ALL PASS (v3 전용 시험 항목 사라짐) ✅
- `can_protocol.hpp` — `kNumPoints = 20`, `0x101 + i (i = 0..19)`, 궤적 상수 없음 ✅
- `install/` 에 v3 잔재 0건 ✅
- 브랜치 `main` @ `c3f01bb`, 커밋 없음 ✅
- ⏳ **못 한 것**: 브리지 배너 실기동 확인 (`TX 0x100+0x101..0x114`). 롤백 시점에 차량
  USB 가 분리돼 `can0` 이 없었다. **차량 재연결 후 아래를 1회 실행할 것:**
  ```bash
  ros2 run bridge_dspace can_bridge_node --ros-args -p can_interface:=can0
  #   기대: bridge up — can0, TX 0x100+0x101..0x114, RX 0x200..0x202
  ```

## 재적용 절차 (dSPACE 가 v3 로 올라간 뒤)

```bash
cd ~/FMA_ws
gh pr checkout 47
source /opt/ros/humble/setup.bash
colcon build --packages-select fma_interfaces bridge_dspace   # --symlink-install 금지
source install/setup.bash
```

**재적용 전 확인:** `candump -n 3 can0` 에 `0x203` 대역이 보이면 dSPACE 도 v3다.
그게 안 보이면 올리지 말 것.

**다시 되돌리려면:**
```bash
git checkout main
rm -rf build/fma_interfaces install/fma_interfaces build/bridge_dspace install/bridge_dspace
colcon build --packages-select fma_interfaces bridge_dspace
```

## 남아 있는 것
- 로컬 브랜치 `codex/can-protocol-v3-mpc-trajectory` (재적용용, 지우려면 `git branch -D`)
- 로컬 태그 `rollback/pre-v3-20260824` → `c3f01bb`
- **main 에는 아무것도 커밋하지 않았다.**

## PR #47 자체에 대한 미해결 지적 (팀장 판단 필요)
1. 20→3점은 CLAUDE.md §3 ② 가 실측으로 금지한 방향 — 실차 재측정 선행
2. 회피 보상 기하(첫 점 7.5cm 의 κ 10~20배 증폭)가 3점+quintic 에서도 성립하는지 미지수
3. **진단용** 궤적 때문에 버스 부하 7% → 76~78%. 10Hz 로 낮추면 ~13%
4. RX(궤적 수신)와 TX(3점 축소)는 성격이 달라 **PR 분리 제안**이 합리적
