# ESTOP 관련 작업 메모

현재 확정된 상태 머신 명칭은 **스테이트 v09.17**이다. 기준 구현·미사용 상태 구분은 [명세](docs/STATE_V09_17.md)를 따른다.

ESTOP/E-stop 관련 분석 또는 변경 작업을 시작할 때
[MGM 상태 머신 변경 메모](docs/MGM_STATE_MACHINE_CHANGE_NOTES.md)의 질문 66·67번 및 후속 결정을 먼저 확인한다.
사용자에게 **독립 E-stop을 삭제하고 상위 제어에 ESTOP 상태를 추가할 예정**이라는
요구사항을 알리고, 현재 구현과 향후 설계를 구분한다.

이 메모는 2026-09-16 상태 머신 요구사항 인터뷰에서 사용자가 요청한 지속 메모다.
인터뷰에서는 코드 변경 없이 전이 조건과 차이점만 정리한다.
ESTOP 내부 회복주행은 구상만 기록한 것이며, 별도의 구현 지시 없이 구현하지 않는다.
이후 사용자가 명시적으로 내리는 변경 지시가 이 메모보다 우선한다.

## 2026-09-16 후속 구현 상태

사용자가 인터뷰 종료 후 코드 변경을 명시적으로 요청했다. `prepare/drive`에서는
독립 E-stop을 실행·구독하지 않고 상위 ESTOP 전이를 사용한다. 과거 프로파일 소스는 보관한다.
[현재 변경 보고서](docs/V2_STATE_MACHINE_CHANGE_REPORT.md)를 함께 확인한다.
후속 “적용 및 병합 진행” 지시로 PR #108 회복 실행기를 상위 ESTOP 계약에 맞춰 구현했다.
[현재 회복 계약](docs/V2_PR108_INTEGRATION.md)을 따른다. 독립 EstopRequest나 옛 시간 기반 후진은 연결하지 않는다. 회피·주차는 사용자 요청대로 개발 중인 모듈 연결을 유지한다.

## 문서·대화 표기

zone 번호는 `[3]`처럼 대괄호로 표기하고, 경로 번호는 `03`처럼 대괄호 없이 표기한다.

## 2026-09-19 ESTOP 후속 변경

[ESTOP 스테이션 계약](docs/ESTOP_STATION_20260919.md)이 과거 PR #108 후진 회복 계약보다 우선한다.
전방 3m × 차폭 구역의 폭 18cm 이상 장애물 진입, 장애물 소실까지 정지,
스테이션 이탈 전 재진입 금지로 변경했다. 내부 후진 회복은 사용하지 않는다.
사용자 지시로 **스테이션 지정 전까지 감지는 비활성화**한다. CSV state 번호/범위는
아직 확정하지 않았으며 내일 지정할 TODO로 남겼다. 임의의 zone/state를 연결하지 않는다.

## 2026-09-20 ESTOP CSV 연결

사용자가 `state=6`을 확정했다. yongin_no_parking.csv의 경로 04 idx 622부터
경로 04 끝(idx 1021)까지 감지 활성화한다. CSV의 state=6부터 같은 path_id
끝까지 ESTOP membership을 생성하고 현재 위치로만 진입한다.
런처는 CSV 자동 선택(-1), 마커가 없는 경로에서는 비활성화다. 전날의
“스테이션 지정 전 전체 비활성화” 메모보다 이 결정이 우선한다.

## 2026-09-20 최종 ESTOP zone [6] 변경

사용자 지시로 state=6 기반 범위를 폐기하고 CSV zone_id=6, inside_zone=1만
ESTOP 감지 구간으로 사용한다. 저장된 경로 04 idx 622~828이다.
구역 이탈은 감지만 비활성화하며, 이미 정지한 ESTOP은 장애물 소실 확인까지
유지한다. 해당 구역 이탈 전 재진입 금지도 유지한다.
