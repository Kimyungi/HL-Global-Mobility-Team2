# run_0830_190720 — 신호등 정지 정상 작동 후 CAN 두절

2026-08-30 19:07 한라대. **신호등·정지선 정지는 정상 작동했고, 그 뒤의 정차는 CAN
케이블 분리로 명령이 dSPACE 에 닿지 않아서**였다는 것을 한 파일 안에서 확인할 수 있게
정합해 둔 통합 로그다.

이 폴더는 **분석 산출물**이다. 원본 로그(`drive_logs/`)는 종전대로 커밋하지 않는다.

## 파일

`run_0830_190720_merged.csv.gz` — 18,450 행 = 184.5 초 × 100Hz, 50 열.

```bash
gunzip -k run_0830_190720_merged.csv.gz
```

## 타임라인

| t_s | 사건 |
|---|---|
| 69.4 | 출발 (`go` 인가) |
| 95.9 | **신호등·정지선 인식** — `traffic_stop=1`. 이때 주행 중이었다 (v_ref 0.895, dSPACE 실측 0.76 m/s) |
| 96.5 | **정지 완료** (v_ref 0). 69.4→95.9 구간 23.1m 주행 |
| 96.5~152.2 | 정지 유지. 위치 변화 **0.01m** |
| **152.28** | **dSPACE RX 두절** (마지막 counter 15268). 그 전 구간에 0.2초 이상 공백이 하나도 없다가 뚝 끊겼다 |
| 161.0 | 초록 → `traffic_stop=0`. MGM 은 재출발을 명령했다 (v_ref 1.0 까지 상승) |
| 161.0~181.5 | **명령은 나갔지만 차는 0.02m 움직였다** — CAN 이 끊겨 dSPACE 에 닿지 않았다 |
| 184.5 | run 종료 (RX 없는 채로 32.2 초) |

`rx_ok` 가 0 으로 바뀌는 시점이 두절 시각이고, 그 뒤 `v_ref` 가 1.0 인데
`gps_lat`/`gps_lon` 이 멈춰 있는 것이 "명령이 전달되지 않았다"의 직접 증거다.

## 정합 방법

- `mgm_snapshots.bin` (인지 입력 전 필드 + 코어 출력) 을 틱 단위로 전개
- `vehicle_vector.csv` (dSPACE RX) 를 **counter 오프셋 40** 으로 붙였다. 15,276 행 중
  15,267 행이 39/40 으로 떨어져 ±1 틱 정확도다 (CLAUDE.md §3 "dSPACE 로그 정합")
- `lateral.csv` (GPS 위경도·품질·횡오차·헤딩·go) 는 10Hz 라 hold 로 채웠다

## 주요 열

`t_s` `state` `v_ref` `traffic_stop` `estop` `immediate_stop` `ref0_x/y/yaw/curv`
**`rx_ok`** (dSPACE RX 수신 여부 — 0 이 된 시점이 CAN 두절) `rx_counter` `rx_v`
`rx_str` `rx_str_ref` `gps_lat` `gps_lon` `gps_quality` `gps_cross_track_m` `go`
