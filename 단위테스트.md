# 센서·인식 로직 단위 확인

각 항목의 **명령 블록 전체를 터미널에 한 번 붙여 넣으면** 해당 화면이 열린다. 자동 판정 테스트가 아니라 실제 센서 영상과 인식 결과를 사람이 확인하는 용도다.

현재 PC의 저장소 경로와 `install_v2` 빌드를 사용한다. 다른 PC에서는 각 명령의 `cd` 경로를 수정한다. 화면이 있는 데스크톱 터미널에서 실행하며, 같은 센서를 사용하는 기존 런쳐는 먼저 `Ctrl+C`로 종료한다. 종료하지 않으면 카메라·시리얼 포트를 중복으로 열 수 없다.

아래 명령은 센서 드라이버·인식 노드·뷰어만 실행한다. MGM, 경로 추종, CAN 송신은 실행하지 않는다. 별도 ROS 도메인 `216`과 로컬 통신을 사용한다. 각 명령은 독립적으로 실행할 수 있고 **실행한 터미널에서 `Ctrl+C`로 종료**한다. 처음 모델을 불러오는 동안에는 영상이 늦게 나타날 수 있다.

## 1. 라이다 4개 → RViz

```bash
env -i HOME="$HOME" USER="${USER:-}" LANG="${LANG:-C.UTF-8}" \
  DISPLAY="${DISPLAY:-}" XAUTHORITY="${XAUTHORITY:-}" \
  PATH=/usr/local/bin:/usr/bin:/bin /bin/bash --noprofile --norc <<'BASH'
set -eo pipefail
cd /home/sangmin/Desktop/HL-Global-Mobility-Team2-v2_main
source /opt/ros/humble/setup.bash
source install_v2/local_setup.bash
export ROS_DOMAIN_ID=216 ROS_LOCALHOST_ONLY=1
exec ros2 launch lidar_fusion_v2 bringup.launch.py rviz:=true
BASH
```

RViz의 `Fixed Frame`은 `base_link`이며, 차량 기준으로 네 라이다의 관측점을 함께 보여준다.

| RViz 표시 이름 | 위치 / 장치 | 색상 |
|---|---|---|
| Raw Front a1 | 앞 / `/dev/lidar_front` | 빨강 |
| Raw Rear a2 | 뒤 / `/dev/lidar_rear` | 노랑 |
| Raw Left b1 | 왼쪽 / `/dev/lidar_left` | 초록 |
| Raw Right b2 | 오른쪽 / `/dev/lidar_right` | 파랑 |
| Unified 4 LiDAR Cloud | 네 라이다 통합 결과 | 하늘색 |

센서별 점이 통합 결과와 겹치면 왼쪽 Displays에서 `Unified 4 LiDAR Cloud` 체크를 끈다. `Raw …` 항목을 하나씩 켜면 각 라이다가 보는 물체와 사각 영역을 구분할 수 있다. 네 방향에 물체를 놓거나 움직여 점이 갱신되는지 확인한다.

## 2. 차선 카메라 → 원영상 위 인식 표시 + 차선 계산 결과

```bash
env -i HOME="$HOME" USER="${USER:-}" LANG="${LANG:-C.UTF-8}" \
  DISPLAY="${DISPLAY:-}" XAUTHORITY="${XAUTHORITY:-}" \
  PATH=/usr/local/bin:/usr/bin:/bin /bin/bash --noprofile --norc <<'BASH'
set -eo pipefail
cd /home/sangmin/Desktop/HL-Global-Mobility-Team2-v2_main
source /opt/ros/humble/setup.bash
source install_v2/local_setup.bash
export ROS_DOMAIN_ID=216 ROS_LOCALHOST_ONLY=1
exec /usr/bin/python3 - <<'PY'
from pathlib import Path
from launch import LaunchDescription, LaunchService
from launch.actions import EmitEvent, RegisterEventHandler
from launch.event_handlers import OnProcessExit
from launch.events import Shutdown
from launch_ros.actions import Node

root = Path.cwd()
lane = Node(package='stack_lane', executable='stack_lane_node', output='screen',
    parameters=[{
        'weights': str(root / 'src/stack_lane/models/yolopv2.pt'),
        'homography_path': str(root / 'src/stack_lane/config/homography.json'),
        'camera_mxid': '14442C105157D3D200',
        'camera_fps': 10, 'usb_speed': 'high', 'device': 'xpu',
        'coeff_smoothing_alpha': 0.3, 'publish_debug_image': True,
    }])
viewer = Node(package='rqt_image_view', executable='rqt_image_view',
    arguments=['/perception/lane_debug_image'], output='screen')
handlers = [RegisterEventHandler(OnProcessExit(target_action=node,
    on_exit=[EmitEvent(event=Shutdown(reason='차선 검사 종료'))]))
    for node in (lane, viewer)]
service = LaunchService()
service.include_launch_description(LaunchDescription(handlers + [lane, viewer]))
raise SystemExit(service.run())
PY
BASH
```

차선 인식 노드와 영상 뷰어가 함께 실행된다. 뷰어를 닫거나 인식 노드가 종료되면 나머지도 종료한다.

| 화면 | 확인할 내용 |
|---|---|
| 왼쪽 카메라 영상 | 빨간 반투명 영역이 모델이 차선이라고 판단한 픽셀이다. 실제 차선과 겹치는지 확인한다. |
| 오른쪽 BEV(위에서 내려다본 화면) | 파랑은 왼쪽 차선, 빨강은 오른쪽 차선, 노랑은 계산한 중앙선이다. |
| 초록 별 `ref_point` | 인식 결과로 계산한 전방 기준점이다. |
| `mode`, `conf` | 차선 추정 방식과 신뢰도다. `mode=none`이면 유효한 차선 추정이 없는 상태다. |

영상은 표시가 없는 원본 별도 창이 아니라 **카메라 영상 위에 판단 결과를 겹친 화면**이다. 뷰어 토픽이 비어 있으면 상단에서 `/perception/lane_debug_image`를 선택한다. `PLACEHOLDER HOMOGRAPHY`가 보이면 차선 위치 계산용 보정값을 확인한다.

현재 주행 기본값과 같이 Intel GPU의 `xpu`를 사용한다. XPU 미지원 PC에서는 위 명령의 `'device': 'xpu'`를 `'device': 'cpu'`로 바꿀 수 있으나 영상 갱신 속도는 느려진다.

## 3. 신호등·정지선 카메라 → 영상 + 빨강/초록 판정 + 정지선 검출

```bash
env -i HOME="$HOME" USER="${USER:-}" LANG="${LANG:-C.UTF-8}" \
  DISPLAY="${DISPLAY:-}" XAUTHORITY="${XAUTHORITY:-}" \
  PATH=/usr/local/bin:/usr/bin:/bin /bin/bash --noprofile --norc <<'BASH'
set -eo pipefail
cd /home/sangmin/Desktop/HL-Global-Mobility-Team2-v2_main
source /opt/ros/humble/setup.bash
source install_v2/local_setup.bash
export ROS_DOMAIN_ID=216 ROS_LOCALHOST_ONLY=1
exec ros2 run stack_traffic stack_traffic_node --ros-args \
  -p camera_backend:=oak -p oak_mxid:=14442C10B167CFD200 \
  -p oak_usb_speed:=high -p oak_fps:=10.0 \
  -p oak_width:=640 -p oak_height:=360 \
  -p oak_exposure_compensation:=0 -p oak_depth_enabled:=false \
  -p model_path:="$PWD/src/stack_traffic/models/yolov8n.pt" \
  -p stopline_model_path:="$PWD/src/stack_traffic/models/stopline_yolov8s_seg.pt" \
  -p yolo_image_size:=640 -p yolo_inference_interval:=2 \
  -p confidence_threshold:=0.185 \
  -p red_phase_yolo_inference_interval:=3 \
  -p halla_stopline_test_enabled:=true \
  -p stopline_yolo_confidence_threshold:=0.30 -p stopline_tracking_confidence_threshold:=0.2 \
  -p stopline_detection_enabled:=true -p stopline_yolo_image_size:=320 \
  -p stopline_stop_y_ratio:=0.0 -p stopline_stop_distance_m:=0.0 \
  -p resume_on_green:=true -p resume_on_red_clear:=false \
  -p resume_on_red_absence:=true \
  -p show_debug:=true -p show_auxiliary_debug:=false
BASH
```

GPS나 zone 진입 없이 신호등 카메라와 인식 노드를 바로 실행한다. 카메라 해상도·노출·모델 입력 크기·추론 주기는 현재 주행 런쳐 기본값에 맞췄다. 정지 임계값은 `0.0`으로 두어 관찰용으로 사용한다.

| 창 / 표시 | 확인할 내용 |
|---|---|
| `traffic_red_binary_test` | 카메라 영상 위 신호등 검출 박스, 빨강/초록 판정, 정지선 위치와 인식 상태 |
| `traffic_light_crop` (보조 창) | 신호등 색상 판단에 사용하는 잘라낸 영역 |
| `red_mask`, `green_mask` | 빨강·초록 후보 픽셀. 최종 판정은 모델·추적·투표 결과도 함께 사용한다. |
| `red_raw`, `green_raw` | 현재 관측에서 빨강·초록으로 판단했는지 여부 |
| `red_votes`, `green_votes` | 최근 관측의 색상 판정 투표 수. 기본은 5회 중 3회다. 확정 적색 단계는 터미널의 `red_phase=1`로 확인한다. |
| 정지선 박스와 굵은 가로선 | 정지선 후보 영역과 가까운 쪽 가장자리 위치 |
| `stopline score`, `stable` | 정지선 검출 점수와 시간적 안정성. `stable=1`이면 안정적으로 검출된 상태이며 노란색으로 표시한다. |
| `stopline_white_mask` | 정지선 처리 결과 마스크. 정지선 처리 결과가 있는 동안 표시된다. |

**한라대 전용 정지선 인식 여부 판단을 위한 임시 테스트 설정이다.** `halla_stopline_test_enabled:=true`로 적색 신호 확정 전부터 정지선 추론을 실행한다. 빨간 신호등 없이 카메라 하단에 정지선을 보여 주고 검출 박스와 `stable`을 확인한다. 신호등 색상 판단도 계속 실행하므로 빨강·초록 신호를 각각 보여 주면서 함께 확인할 수 있다. 기존 적색 확정 후 정지선 검출 방식으로 복구하려면 이 값을 `false`로 바꾼다.

한라대 주행의 “정지선만으로 상태 진입 → 최초 인식 후 3초에 탈출”은 MGM의 임시 스테이트 전이조건이다. 이 단독 명령은 인식 노드만 실행하므로 3초 타이머와 차량 상태 전이는 시험하지 않는다.

`FINAL_STOP`은 인식 노드 내부의 정지 요구 판단값이며, 이 명령으로 차량을 정지·출발시키지는 않는다. 여기서는 정지 임계값을 껐으므로 `FINAL_STOP=0`만 보고 검출 실패라고 판단하지 말고 검출 박스·투표·`stable`을 확인한다. 영상 창에 포커스를 둔 상태에서 `q`를 눌러도 종료된다.

보조 창이 메인 영상을 가리지 않도록 기본 명령은 `show_auxiliary_debug:=false`로 설정했다. `traffic_light_crop`, `red_mask`, `green_mask`, `stopline_white_mask`가 필요할 때만 `true`로 바꾼다. 정지선은 최초 신뢰도 `0.30` 이상 검출 후, 위치·크기가 이어지는 후보는 `0.2` 이상까지 추적한다. 정지선 추론에서 3회 연속 놓치면 추적을 해제하고 다시 `0.30` 이상이 필요하다. 최근 5회 중 3회 검출 및 세로 위치 안정성 조건은 유지한다.

신규 신호등 검출 기준은 `0.185`, 추적 중인 신호등의 연속 검출 기준은 `0.10`이다.

## 학습용 원본 사진 촬영

위 3번 인식 노드를 실행한 상태에서 별도 터미널에 아래 명령을 입력하면, 글씨·박스가 없는 `/perception/traffic_image_raw` 영상을 PNG 200장 저장한다. **이 명령을 실행하는 순간 촬영이 시작된다.** 저장 폴더는 `captures/traffic_raw_날짜_시간`이다.

```bash
bash --noprofile --norc <<'BASH'
set -eo pipefail
cd /home/sangmin/Desktop/HL-Global-Mobility-Team2-v2_main
source /opt/ros/humble/setup.bash
source install_v2/local_setup.bash
export ROS_DOMAIN_ID=216 ROS_LOCALHOST_ONLY=1
exec /usr/bin/python3 scripts/capture_traffic_raw.py --count 200 --fps 10
BASH
```

`--fps 10`은 최대 저장 빈도다. 인식 노드가 카메라에서 읽은 새 프레임만 저장하며, 추론 속도에 따라 실제 저장 FPS는 낮아질 수 있다. 프레임을 복제해 10fps로 채우지 않는다. 실제 속도·장수는 `capture_info.json`, 프레임별 시간은 `timestamps.csv`에 기록된다. 임계값이나 모델 변경 전후의 비교에는 표시 없는 이 원본을 사용한다.

## 화면이 나오지 않을 때

- `package not found` 또는 설치 경로 오류: 저장소에서 `./scripts/v2 build` 후 다시 실행한다.
- 카메라 열기 실패: 기존 prepare/drive 또는 다른 카메라 프로그램을 종료하고, USB 연결과 위 명령의 카메라 MxID를 확인한다.
- RViz에 특정 방향만 보이지 않음: 해당 `/dev/lidar_*` 장치 연결과 터미널의 드라이버 오류를 확인한다.
- 창은 열리지만 결과가 없음: 카메라가 실제 차선/신호등을 향하는지, 모델 로딩이 끝났는지 확인한다. 위 한라대 임시 테스트 설정에서는 적색 신호 없이 정지선 검출을 확인할 수 있다.

문서의 명령 문법과 실행 대상은 저장소·설치본을 기준으로 확인했다. 실제 센서 연결 상태에서의 영상 표시와 검출 품질은 현장에서 확인해야 한다.
