# stack_avoid — 측정·설정값 정리 (M3: 정적 장애물 회피)

> 실측값의 단일 소스는 `config/params.yaml`. 이 문서는 각 값의 **측정 방법**을 설명한다.
> 결정 사항: 제원=직접 실측 / 원점=후축 중심(파라미터로 변경 가능) / LiDAR=수평·정면 정렬 / 시나리오=정적 장애물.

## A. LiDAR 장착 (원점→LiDAR, 수평·정면 정렬 → x·y·z만 측정)

수평·정면 정렬이므로 yaw/roll/pitch = 0 고정. launch TF도 이에 맞춰 갱신 필요
(`~/ydlidar_ros2_ws/.../launch/ydlidar_launch.py:49` 의 `base_link→laser_frame`).

| # | 값 | 단위 | 측정 방법 |
|---|---|---|---|
| A1 | lidar_mount.x_m | m | 후축 중심(좌우 뒷바퀴 축의 중앙)에서 LiDAR 원점까지 **전방 수평거리**를 줄자로 측정 |
| A2 | lidar_mount.y_m | m | 차량 중심선에서 LiDAR의 **좌우 편차**. 중앙 정렬이면 0 |
| A3 | lidar_mount.z_m | m | 지면에서 LiDAR **스캔 평면 높이**를 줄자로 측정 |

## B. 차량 제원 (직접 실측)

| # | 값 | 단위 | 측정 방법 |
|---|---|---|---|
| B1 | vehicle.width_m | m | 줄자로 **타이어 외측 폭**(가장 넓은 부분). 사이드미러 포함 시 `mirror_included: true` |
| B2 | vehicle.length_m | m | 앞범퍼~뒷범퍼 전장 |
| B3 | vehicle.wheelbase_m | m | **앞바퀴 중심~뒷바퀴 중심**(같은 쪽) 거리 |
| B4 | vehicle.front_overhang_m | m | 앞범퍼~앞축 중심 |
| B5 | vehicle.max_steer_deg | deg | full lock 시 **앞바퀴 각도**(각도앱/각도기) 또는 조향 서보 스펙 |
| B6 | vehicle.min_turn_radius_m | m | full lock 원 주행 후 반경 측정, **또는** `wheelbase / tan(max_steer)` 로 계산 |

## C. 회피 판단 파라미터 (정적 장애물 — 초기값 설정 후 주행 튜닝)

물리 측정이 아닌 설계·튜닝값. B1과 시연 속도가 정해지면 파생 계산 가능.

| # | 값 | 단위 | 산정 방법 |
|---|---|---|---|
| C1 | avoid.roi_angle_deg | deg | 전방 관심영역 ±각도. 기본 60° 제안 |
| C2 | avoid.max_range_m | m | 기본 12.0 (센서 한계) |
| C3 | avoid.ttc_stop_s | s | 즉시정지 임계. 제동거리+반응 고려, 초기 제안값 논의 |
| C4 | avoid.lateral_margin_m | m | 측방 안전여유. 측방여유판정 = width/2 + margin, 통과최소폭 = width + 2·margin |
| C5 | avoid.target_speed_mps | m/s | **M3 시연 주행 속도** (TTC 계산 기준) — 실측/목표값 |

## D. LiDAR 스캔 특성 (✅ 실측 완료 — YDLIDAR **T-mini Plus**)

| 항목 | 값 |
|---|---|
| 스캔 주파수 | 10.07 Hz (지터 std ~0.4ms) |
| 각도 해상도 | 0.839° |
| 각도 범위 | ±180° |
| 거리 범위 | 0.03 ~ 12.0 m |
| 포인트/스캔 | 약 429점 |

> 모델명 정정 (2026-07-31): 이전에 "T-mini Pro"로 적혀 있었으나 **T-mini Plus**가 맞다.
> 드라이버 설정(`Tmini.yaml`)은 이 하드웨어로 정상 동작 중이므로 값 변경은 없다.
>
> ⚠ **주기 재확인 필요** — 2026-07-30 관측 시 `/scan`이 **6.11 Hz**(min 0.162s / max 0.166s)로,
> 위 실측치 10.07 Hz와 다르게 나왔다. `fixed_resolution: true`라 포인트 수는 430으로 고정된 채
> 주기만 떨어진 형태. 다음 연결 시 `ros2 topic hz /scan`으로 확인할 것.

## E. 실행·검증 커맨드

```bash
source /opt/ros/humble/setup.bash
source ~/ydlidar_ros2_ws/install/setup.bash
ros2 launch ydlidar_ros2_driver ydlidar_launch.py \
  params_file:=$HOME/ydlidar_ros2_ws/src/ydlidar_ros2_driver/params/Tmini.yaml
# 검증: ros2 topic hz /scan  /  ros2 topic echo /scan --once  /  rviz2
```

---

# F. 실차 측정 세션 2026-08-07 (스탠드)

bag: `~/avoid_logs/field_sweep_20260807_151232`, `~/avoid_logs/field_vref_20260807_152046`
구성: `field_session.launch.py mode:=avoid` (LiDAR + stack_avoid + stack_estop + avoid_to_ref + CAN)

## F-1. ★ 횡방향 명령이 dSPACE에 반영되지 않음 (최우선 이슈)

같은 10ms CAN 버스트로 나가는 두 명령 중 **종방향만 추종되고 횡방향은 무반응**이다.

| 스윕 구간 | v_ref 명령 | v 실측 | κ 명령 | str 실측 |
|---|---|---|---|---|
| target_speed_mps=0.2 | 0.200 | **0.200** ✓ | 0.127 | −0.0353 |
| target_speed_mps=0.4 | 0.400 | **0.393** ✓ | 0.127 | −0.0352 |
| target_speed_mps=0.6 | 0.600 | **0.591** ✓ | 0.127 | −0.0354 |
| target_speed_mps=0.8 | 0.800 | **0.791** ✓ | 0.127 | −0.0108 |
| curvature_gain=1.0 | 0.200 | 0.200 ✓ | 0.127 | −0.0109 |
| curvature_gain=3.0 | 0.200 | 0.200 ✓ | **0.381** | −0.0108 |
| curvature_gain=4.5 | 0.200 | 0.200 ✓ | **0.572** | −0.0109 |
| curvature_gain=6.0 | 0.200 | 0.180 ✓ | **0.762** | −0.0287 |

```
종방향  v_ref → v     상관 +0.843   (n=10148)
횡방향  κ    → str    상관 −0.166   ← κ를 6배(0.127→0.762) 흔들어도 무반응
```

`lookahead_m` 스윕(별도 세션)도 동일: ref y를 **48배**(0.012→0.568 m) 바꿨는데
str 중앙값이 −0.0265로 소수점 4자리까지 불변. estop 0%·v_ref 0.2로 조건도 깨끗했다.

**PC 송신은 검증 완료 (문제 아님):**
- `can_bridge_node` 코드: `0x101`에 x·y·yaw·curvature 양자화 송신 → 헤더 `0x100` 마지막
- 실측: TargetRef 500건 → CAN 프레임 **1000개 = 사이클당 정확히 2.00**, TX 에러 0·드롭 0
- 양자화도 정상: κ 0.127 → raw 254, y 0.0102 → raw 10, yaw 0.0508 → raw 508 (전부 유효 범위)

**액추에이션도 정상:** 세션 중 str이 −18.42° ~ +8.44°까지 실제로 움직였다.
8/6의 "조이스틱 전원 off로 str 고정" 실패 모드가 아니다.

→ **문제는 `0x101` 수신 이후(dSPACE 측).** 디코딩 여부, 또는 Generate_Trajectory·MPC가
   REF_POINT_00의 y·yaw·curvature를 실제로 경계조건으로 쓰는지 확인 필요 (손상민).
   이것이 풀리기 전에는 ①(조향 응답)·②(측방 이동 곡선) 측정이 의미가 없다.

## F-2. 기각된 가설 2건

- **lookahead_m (송신 점 위치)** — 무효. ref y 48배 변화에 str 무반응
- **MPC 지평 (0.2s × v_ref)** — 무효. v_ref를 4배(0.2→0.8) 올려도 str 무반응
  (v는 정확히 추종했으므로 v_ref 자체는 도달함)

## F-3. 감지 신뢰 거리 (③) — 각 마크 이후 55초, 콘 이동 구간 제외

| 콘 위치 | gap | 감지율 | 목표점 산출 | narrow_gap | 목표 y | 좌우 뒤집힘 |
|---|---|---|---|---|---|---|
| 3 m | 3.126 ± 0.228 | 100% | 100% | 0% | +0.494 ± 0.110 | 6회 / 552 |
| 2 m | 2.114 ± 0.002 | 100% | 100% | 0% | +0.642 ± 0.009 | 0회 / 552 |
| 1 m | 1.113 ± 0.002 | 100% | **73.9%** | **26.1%** | −0.849 ± 0.009 | 0회 / 408 |

- **감지 자체는 전 거리 100%** — 라이다는 문제없음
- **1m에서 목표점 산출이 26% 실패**(narrow_gap). 가까울수록 같은 콘이 더 넓은 각도를
  차지해 `offset_max`(1.0m) 안에 통과 가능한 열림이 안 남는다
- **3m는 gap 표준편차 0.228 + 좌우 뒤집힘 6회** — 클러스터링이 흔들리는 경계
- **2m가 가장 안정** (표준편차 0.002, 뒤집힘 0)
- 2m와 1m에서 목표 y 부호가 반대(+0.642 / −0.849). 좌우 열림이 비슷하면
  `min(reach, key=abs)`가 근소차로 뒤집힌다 → **stage2에서 히스테리시스 필요**

## F-4. ★ detect_range_m 이 감지 상한을 지배한다

콘을 정면 3m에 놓았을 때 **감지율 0%**. 원인은 센서가 아니었다:

```
라이다 원시 스캔  정면 ±6°에 3.18~3.21 m 반사점 8개, 10/10 스캔 안정
통로 내 최근접점  x = 3.141 m
detect_range_m    3.0            ← 3.141 > 3.0 이라 전량 폐기 (node.py:213, 266)
```

`detect_range_m`을 3.5로 올리자 즉시 **감지 100%** 복구. 센서 여유는 충분(스펙 12m).

**stage2 영향:** avoidable 임계가 요구하는 감지 거리는
`0.5 m/s → 1.75 m` (여유 있음) / `0.8 m/s → 2.8 m` (3.0에 거의 닿음).
**속도를 올리면 `detect_range_m` 3.0이 avoidable의 병목이 된다.**

## F-5. 방법론 메모

- `/test/event` 구간은 **다음 마크까지**이므로, 콘을 옮기는 시간이 직전 구간에 섞인다.
  3m·2m 구간의 gap 표준편차가 0.5 근처로 나온 원인. 측정 종료 시 종료 마크를 찍거나
  분석에서 앞 N초만 쓸 것 (위 F-3은 앞 55초로 재계산한 값)
- 스윕 첫 마크 유실 → `/test/event` 구독자 매칭 대기 추가로 수정 완료

## F-6. 원인 격리 — stack_avoid 로직 배제, 점 개수 배제 (2026-08-07 후속)

"내 로직 문제인가"를 가리기 위해 **stack_avoid를 완전히 배제하고** 팀 자체 시험 도구
`bridge_dspace/dummy_ref_publisher`로 직접 송신했다 (`state=LANE`, v_ref=0.2).

| κ 명령 | 기대 조향 | n_points=1 | n_points=20 |
|---|---|---|---|
| 0.0 | 0.00° | −1.79° | — |
| +0.5 | **+16.57°** | −1.79° | −1.52° |
| −0.5 | **−16.57°** | −0.60° | −0.55° |
| +0.8 | **+25.45°** | −1.53° | −1.56° |

**κ 부호 교대 4회 (n_points=1):**

```
+0.5 → −1.56°   −0.5 → −0.57°
+0.5 → −0.56°   −0.5 → −0.56°
+0.5 → −0.57°   −0.5 → −0.57°
+0.5 → −0.57°   −0.5 → −0.56°
```

첫 회차 이후 **str이 −0.56~−0.57°에 완전히 고정**. 8회 중 7회가 소수점 둘째 자리까지 동일.
기대 진폭 33.14°에 대해 실측 차이 0.25°(= 표류 수준). **횡방향 응답 없음.**

### 배제된 원인 (전부 실측)

| 대상 | 시험 범위 | 결과 |
|---|---|---|
| `lookahead_m` | 0.05 → 2.8 (56배) | 무반응 |
| `curvature_gain` | 1.0 → 6.0 | 무반응 |
| `target_speed_mps` | 0.2 → 0.8 | 무반응 (v는 1.5% 이내 정확 추종) |
| `state` | AVOID(2) vs LANE(0) | 둘 다 무반응 |
| **점 개수** | **1 vs 20** | **차이 없음** |
| **stack_avoid 코드** | **레퍼런스 도구로 대체** | **동일 실패** |
| CAN 송신 | 사이클당 2.00 프레임 | 에러 0·드롭 0 |
| 액추에이션 | 세션 중 str −18.42°까지 실동 | 정상 |
| 실물 육안 확인 | 스탠드에서 직접 관찰 | 안 꺾임 (판독값과 일치) |

→ **PC 측에서 바꿀 수 있는 것은 전부 배제됐다.** 종방향(v_ref)은 같은 CAN 버스트로
   완벽히 도달·추종되는데 횡방향만 무반응이다. dSPACE 측 횡방향 경로
   (0x101 디코딩 / Generate_Trajectory / MPC 횡방향 가중) 확인 필요 — 손상민.

**⚠ 이 이슈가 stage2 전체를 막고 있다.** ①(조향 응답)·②(측방 이동 곡선)·ⓐⓒ(경계 시험)
모두 조향이 명령을 따르는 것이 전제다.

## F-7. ★ dSPACE 모델(FMA_rev1.slx) 직접 분석 — 조향 무반응의 구조적 원인

`~/다운로드/FMA_rev1.slx`(=`FMA_rev1/` 에 이미 전개됨)를 직접 열어 확인했다.

### 배선은 정상

```
CAN_Input → Bus Selector1 ┬ out1~5 (TARGET_HEADER.counter/state/n_points/v_ref/reserved)
                          │        → Bus Creator1 → CAN_Processor → v_ref → MPC in:1
                          └ out6~9 (REF_POINT_00.x / .y / .yaw / .curvature)
                                   → Bus Creator ────────────────────→ MPC in:2 (ref_point)
```

`REF_POINT_00`의 4필드가 정확히 MPC로 들어간다. **PROTOCOL.md와 신호명·경로 모두 일치.**
(주의: `CAN_Processor`는 헤더만 처리하고 v_ref만 낸다. ref_point는 이 블록을 거치지 않는다.)

MPC_Controller = Stateflow 차트(`chart_93.xml`), `Generate_Trajectory()` → `MPC()` 순차 호출.

### 원인 1 — 궤적 미리보기 거리가 v_ref에 비례해 붕괴한다

`Generate_Trajectory`:
```matlab
pointSpacing = sampleTime * abs(v_ref);      % 10ms × v
requestedArc = (0:predictionHorizon).' * pointSpacing;
```

| v_ref [m/s] | 점 간격 | **궤적이 덮는 거리** |
|---|---|---|
| 0.2 | 2 mm | **0.04 m** |
| 0.8 | 8 mm | 0.16 m |

회피 기하는 0.4~2.9 m 스케일인데 MPC가 보는 것은 **앞 4 cm**뿐이다. 그 구간의 곡률은
quintic 시작 경계조건 `curvature0 = tan(vehicle_vector.steering)/wheelbase`
(= **현재 조향**)에 지배된다 → 레퍼런스가 사실상 "지금 조향을 유지하라"가 된다.

### 원인 2 — 제어 벌점이 궤적 추종항을 압도한다

`MPC()`: `R = 500·I`, `F = 200·I`, `Q = kron(I, diag([200 300 500]))`,
`B_k = [0; 0; v·Ts/wheelbase]`, `Θ ∝ B_k`

| v_ref | B_k | 추종항 B²·Q | 제어벌점 R | 비율 |
|---|---|---|---|---|
| 0.2 | 0.00336 | 5.6e-3 | 500 | **1.1e-5** |
| 0.8 | 0.01345 | 9.0e-2 | 500 | 1.8e-4 |

추종항이 벌점과 맞먹으려면 v ≈ wheelbase/Ts = **59.5 m/s** 가 필요하다(비현실).
저속에서는 `quadprog` 최적해가 dU ≈ 0으로 눌린다.

### 실측이 이 구조와 정확히 일치한다

오늘 str이 기준선에서 벗어난 것은 **전체의 0.38%, 단 0.9초**뿐이었고 그 구간은 **전부 v_ref = 0**:

```
t=238.5s  str −4.57°   v_ref 0.000
t=238.9s  str −18.42°  v_ref 0.000     ← 약 30°/s (MPC duMax 50°/s 이내)
t=239.4s  str −7.54°   v_ref 0.000
```

v_ref = 0 이면 `B_k = 0 → Θ = 0` 으로 궤적 추종항이 **완전히 소멸**하고,
동시에 `pointSpacing ≤ 1e-12` degenerate 분기가 발동해 전 궤적점 곡률이 `curvature0`가 된다.
남는 구동력은 `EU_k = (이전 명령) − (실측 조향)` 뿐인데 이 항은 **B_k 스케일이 없어**
그대로 작동한다 → **v_ref=0 에서만 조향이 움직인다.** 실측과 일치.

### 추가로 확인된 것

- `if isempty(dU) → outWheelSteering = wheelSteering` — QP 실패 시 **직전 조향을 무진단으로 유지**.
  진단 신호가 없어 실패해도 알 수 없다
- 지평 4cm 안의 곡률은 `lookahead`·`curvature_gain` 조합에 극도로 민감하다(모델 재현 계산):
  `(0.4, 1) → steeringRef 3.03°` / `(0.05, 6) → −0.11°(상쇄)` / `(0.02, 6) → 24.56°`
  우리가 시험한 조합들이 하필 레퍼런스가 작거나 상쇄되는 구간이었다

### → 손상민 확인 요청 지점 (dSPACE 측)

1. `pointSpacing = Ts × v_ref` 를 **미리보기 거리 기반**으로 분리 (예: 0.1~0.5 m 간격 고정)
2. `R`·`F` 대비 `Q` 가중 재조정 — 현 조합은 운용 속도에서 추종항이 1e-5로 눌린다
3. `isempty(dU)` 경로에 진단 출력 추가

## F-8. 조향 살리기 — lookahead 클램프와 남은 문제 (2026-08-07 저녁)

### 수정 1 — lookahead 를 MPC 미리보기 창 안으로 (preview_frac, 기본 0.5)

dSPACE `Generate_Trajectory` 의 미리보기 = `N_p × Ts × v_ref` (v=0.2 → **4cm**).
avoid_to_ref 의 `lookahead_m=0.4` 는 그 **10배 밖**이라 MPC 샘플 구간에 들어오지 않았다.

**A/B 실측 (스탠드, v_ref>0 샘플만, 1200개씩, 2회 반복):**

| preview_frac | 송신 ref x | str 중앙 |
|---|---|---|
| 0.0 (기존) | 0.3995 | **−0.990°** |
| 0.5 (수정) | 0.0200 | **+2.961°** |
| 0.0 (기존) | 0.3993 | **−0.951°** |
| 0.5 (수정) | 0.0200 | **+2.637°** |

**고정돼 있던 조향이 살아났다.** 명령 κ=0.105 의 이론값 3.58° 와도 근접.

### 수정 2 — 횡방향 부호 (lateral_sign, 기본 −1)

인지가 낸 목표점 y 는 RViz 표시와 일치하는데(인지 프레임 정상), 그대로 보내면
바퀴가 반대로 꺾인다 → 반전은 **명령→서보 구간**에 있다.
⚠ 인지 쪽 스캔 부호(`scan_lateral_sign`)를 뒤집어 고치려 했으나 **목표점이 엉뚱한
구역에 찍혀 원복**했다. 인지는 건드리지 말 것.

### 수정 3 — RViz 차량 정면 화살표 + LEFT/RIGHT 라벨 (avoid_viz)

좌우 규약 확인용. `vehicle_forward` (노란 화살표, 전방 1m) + `side_label`.

### ★ 남은 문제 — 응답이 단조롭지 않다

| v_ref | preview_frac | lookahead | str 중앙 | 범위 |
|---|---|---|---|---|
| 0.2 | 0.5 | 2 cm | **−5.12°** | 좁음 (확인된 정상 방향) |
| 0.2 | 1.0 | 4 cm | −2.99° | −5.14 ~ −1.30 (흔들림) |
| 0.2 | 2.0 | 8 cm | **+4.48°** | ← 부호 반전 |
| 0.6 | 0.5 | 6 cm | **+4.48°** | ← 부호 반전 |
| 0.6 | 1.0 | 12 cm | +5.92° | −1.51 ~ +6.23 (흔들림) |
| 0.6 | 2.0 | 24 cm | +5.90° | 좁음 |

- **lookahead 4~6cm 를 넘으면 조향 부호가 뒤집힌다.**
- `preview_frac` 이 lookahead 를 속도에 비례해 늘리므로, **속도를 올리면 자동으로
  그 경계를 넘어 반대로 꺾인다** (v 0.2→0.4→0.6 에서 str −4.99 → −2.69 → −0.89).
- `curvature_gain` 1.0→2.5 (κ 0.25→0.63, 2.5배) 에서 str 이 5.77° 에 고정 —
  **크기도 명령에 비례하지 않는다.**
- 0.2 이하로 줄이면 chord 가 2mm 까지 줄어 quintic 이 무너지고 역시 부호가 뒤집힌다.

**확보된 안정 동작점은 `v_ref=0.2 · preview_frac=0.5` (lookahead 2cm) 하나뿐이다.**
속도를 올리려면 이 비선형 응답을 먼저 풀어야 한다. 원인은 F-7 의 구조 —
미리보기가 4~24cm 로 짧아, 어느 파라미터를 쓰든 매우 짧은 호의 어느 조각을
샘플하느냐만 바뀔 뿐이다.

## F-9. ref 점 생성 방식 정리 (2026-08-07 저녁)

dSPACE 는 `REF_POINT_00` 한 점 + 현재 차량 상태로 quintic 전체를 만들고,
그 앞 `N_p × Ts × v_ref` m 만 샘플해 조향을 낸다(v=0.2 → 4cm). 따라서 **보내는 점의
위치가 chord 길이를 정하고, chord 가 그 4cm 안에서 곡률이 얼마나 변하는지를 정한다.**

### ★ 부호 함정 — 현재 조향이 반대쪽일 때

quintic 시작 곡률 = `tan(현재 조향)/wheelbase` 다. 목표가 왼쪽인데 바퀴가 오른쪽이면
경로는 오른쪽에서 출발해 왼쪽으로 넘어가는데, **넘어가는 지점이 chord 에 비례한다.**

목표점 (2.208, +0.933) κ_target +0.325 / 현재 조향 −5° (κ0 −0.147):

| 송신점 | chord | 0cm | 4cm | 8cm | 12cm | 부호 0 교차 |
|---|---|---|---|---|---|---|
| (0.40, +0.03) 짧은 lookahead | 0.40 m | −0.147 | **+0.230** | +0.458 | +0.565 | **1 cm** |
| (2.21, +0.93) 목표점 직송 | 2.40 m | −0.147 | **−0.073** | −0.005 | +0.058 | **9 cm** |

v=0.2 에서 MPC 는 4cm 만 본다 → 목표점 직송은 그 지점이 아직 음수라 **"오른쪽 유지"**
로 읽힌다. 자기강화 루프가 된다(바퀴가 오른쪽 → 궤적 시작 오른쪽 → 오른쪽 유지 명령).
**하루 종일 str 이 −0.57° 에 눌러붙어 있던 현상, 파라미터를 흔들 때마다 부호가 튀던
현상이 모두 이것으로 설명된다.**

### 세 방식의 모델 예측 (같은 상황, 현재 조향 0°)

| gap 1.45m, 목표점 (2.208,+0.933) | v=0.2 | v=0.4 | v=0.6 |
|---|---|---|---|
| 기존 (lookahead 0.4m) | +8.05° | +12.65° | +14.62° |
| 목표점 직송 (현재 기본) | +1.78° | +3.43° | +4.94° |
| 역산 scale_match | +11.5° | +12.2° | +12.9° |

현재 조향이 −5° 이면 목표점 직송만 부호가 뒤집힌다(−2.50° / −0.17° / +1.99°).

### 현재 구성

- **기본: 인지 회피 목표점을 그대로 송신**(`pullback_ratio=0`). 위치는 손대지 않고
  그 점으로 가는 등곡률 호의 `yaw = 2·atan2(y,x)`, `curvature = 2y/(x²+y²)` 만 채운다.
  (둘을 0으로 두면 quintic 이 S자가 되어 초기 조향이 ≈0 이 된다)
- `pullback_ratio > 0` : 전장 × 비율만큼 x 를 당김(y 유지)
- `scale_match=true` : 미리보기 지점에서 필요 곡률이 나오도록 x 를 역산
- 호-lookahead 경로와 파라미터(`lookahead_m`·`preview_frac`·`lookahead_max_m`)는 제거

⚠ 위 표는 전부 **모델 예측**이다. 실차 검증은 PCAN-USB 재연결 후.

## F-10. 조향 크기의 설계값 = 세로 여유 (clear_before_m)

"조향각이 부족하다"의 원인을 분해하면 두 가지였다.

### ① 요구값 자체가 작았다 — 세로 여유가 0이었기 때문

회피 목표점은 `(장애물의 x, 통과 열림의 y)` 다. 그대로 쓰면 **"장애물에 도달하는
그 순간 딱 옆에 있어라"** = 세로 여유 0 이다. 그 순간에 겨우 비켜 있으면 차 옆면이
장애물을 스친다.

요구 곡률 `κ = 2y / ((x_obs − clear_before)² + y²)` — 장애물 2.96m·측방 0.93m 기준:

| clear_before | 요구 조향 | 의미 |
|---|---|---|
| 0.00 m | 6.56° | 도달 순간 겨우 비킴 (여유 0) |
| 0.43 m | 8.66° | 전장 절반 먼저 |
| **0.85 m** | **11.76°** | **전장 하나 먼저 ← 기본값** |
| 1.20 m | 15.60° | |
| 1.60 m | 22.18° | |

### ② 폐루프는 정상이다 — 요구값에 수렴한다

매 주기 궤적이 "그때의 조향"에서 다시 출발하므로 조향이 계단식으로 오른다.
폐루프 시뮬레이션(duMax 0.5°/step 포함):

| 배치 | 기하 요구 | 수렴값 | 도달 |
|---|---|---|---|
| gap 1.45m | 10.94° | 12.02° | ~0.5 s |
| gap 2.6m | 4.42° | 4.56° | ~0.5 s |

**제어 문제가 아니다.** 다만 실측 1건은 요구 8.53° 대비 5.77°(68%)로, 이 잔여
32% 는 아직 설명되지 않았다(표본 1개, 재측정 필요).

### 접근 중 조향은 "커지는 구조"가 아니다 — 뒤처질 때만 커진다

목표점을 세계좌표에 고정하고 접근 시뮬레이션:

| | t=0 | t=2s | t=4s |
|---|---|---|---|
| 완벽 추종 | 6.56° | 6.56° | 6.55° | ← 같은 원 위이므로 κ 불변 |
| 절반만 추종 | 6.56° | 8.02° | 10.33° | ← 측방이 안 줄어 자동 보정 |

즉 자기교정형이다. 앞서 "가까워질수록 커진다"고 적은 것은 차가 옆으로 전혀
안 움직인다고 가정한 값이라 과장이었다.

### 구조 정리

```
① clear_before_m = 0.85   안전 여유(설계 의도) → 요구 곡률을 정한다
② scale_match    = true   그 곡률이 MPC 미리보기 지점에서 실제로 나오도록
                          송신점 x 를 역산 (y 는 항상 목표 그대로)
```

②가 없으면 요구 곡률 지점을 그대로 보내게 되는데, MPC 가 보는 4cm 구간은 아직
시작 경계조건(현재 조향) 근처라 실행 조향이 **부호까지 뒤집힌다**:

| clear_before 0.85 | v=0.2 | v=0.6 |
|---|---|---|
| scale_match=true | **+12.42°** | **+13.89°** |
| scale_match=false | −2.04° | +4.54° |

조향 크기를 바꾸려면 `clear_before_m` 하나만 만지면 된다.

## F-11. ★ GPS bag 대조 — yaw 가 조향을 죽이고 있었다 (2026-08-07 밤)

김윤기 릴리즈 `gps-bags-20260807` (위빙 run / 정상 run + candump) 를 받아 대조했다.
**같은 dSPACE 에 GPS 가 무엇을 보냈을 때 조향이 얼마나 나왔는가**를 직접 비교한 것.

### GPS 가 보내는 것 vs 우리가 보내던 것

| | GPS S자(정상) | GPS 직선(위빙) | 회피(기존) |
|---|---|---|---|
| `ref[0].x` | +0.928 | −0.024 | +0.80~1.07 |
| `ref[0].y` | +0.000 (\|y\| 평균 0.153) | −0.048 (\|y\| 평균 0.384) | +0.71~0.93 |
| **`ref[0].yaw`** | **−0.2°** | **+0.0°** | **+60~85°** |
| `ref[0].curv` | −0.004 (범위 ±0.65) | +0.004 (범위 ±0.096) | +0.87 |
| 실측 str | ±27.9° | ±28.6° | **1.3~5°** |

**위빙 run 이 결정적이다**: 송신 κ 등가가 최대 3.26° 인데 실측 조향은 ±28.6° 로 포화했다.
→ dSPACE 조향은 `curvature` 필드가 지배하지 않는다.

### 실측 상관 (지연 0.3s 보정)

| 입력 후보 | S자 | 위빙 |
|---|---|---|
| `atan2(y,x)` (점의 방향) | **−0.952** | **−0.762** |
| `y` | −0.945 | −0.555 |
| `yaw` | −0.724 | −0.084 |
| `curvature` | −0.394 | −0.331 |
| `x` | −0.014 | −0.051 |

`curvature` 필드는 상관이 약하다. 조향은 **점의 기하**에 반응한다.

### ★ 실차 검증 — yaw 만 바꿔 5배

κ 를 −0.870 으로 고정한 채 `yaw` 만 스윕 (스탠드, 25s 정착 후 측정):

| 송신 yaw | 실측 str |
|---|---|
| −82.8° (호 접선각, 기존) | **1.29°** |
| −41.3° | 5.58° |
| **0°** (GPS 방식) | **6.8°** |

2회 재현. GPS 는 `yaw` 에 **트랙 접선**(≈0°)을 넣는데 우리는 `2·atan2(y,x)` =
**목표점 도달 시 헤딩**을 넣었다. 측방 오프셋이 크면 80°가 넘는다.
dSPACE quintic 에게 yaw=−83° 는 "끝점에서 거의 옆을 보라"는 뜻이고, 그러면 경로가
처음엔 완만하다 뒤에서 급히 꺾이는 형상이 되어 **MPC 가 보는 앞 4cm 가 오히려 평평해진다.**

의미상으로도 yaw=0 이 옳다 — 회피를 마치면 **원래 진행 방향과 나란**해야 한다.

### y 의존성은 약하다 (yaw=0 고정)

| 송신 y | atan2(y,x) | str |
|---|---|---|
| −0.703 | 41.3° | 8.46° |
| −0.352 | 23.8° | 7.78° |
| −0.176 | 12.4° | 6.21° |
| −0.106 | 7.5° | 5.18° |

y 를 6.6배 줄여도 str 은 8.5→5.2° 로 완만하게만 변한다(포화 응답).

### 배제 확정

`CAN_Processor` 차트 전문은 `v_ref = header.v_ref` 한 줄뿐 — `n_points`·`state`·
`counter` 를 **아예 읽지 않는다.** 점 개수는 조향과 무관함이 최종 확인됐다.

### 남은 격차

`yaw=0` 으로도 명령 등가 27.4° 대비 실측 6.8° (4분의 1). 미해결.
스탠드 각도만으로는 회피 성립 여부를 알 수 없으므로 지상 주행이 다음 단계다.

---

# G. 다음 세션 인수인계 (2026-08-08 작성)

## 목표 (이기돈 지시, 6개 조건)

1. 팀장(김윤기) GPS 미션에서는 **MPC 문제가 전혀 발견되지 않음**
2. **RViz 에 찍히는 점(초록점) 방향으로 정확히 조향**하고 싶다
3. 지금까지 한 번도 성공하지 못했다
4. 목표는 **초록점으로 즉시 조향**
5. 현재 로직을 분석해 위 조건을 만족시킬 것
6. **dSPACE 수신 양식(점 1개)을 철저히 유지**할 것

## 현재 구성 (이 조건들을 만족하도록 설정됨)

```
send_target_as_is = true     송신 위치 = 초록점 그대로 (요구 2·4)
n_points          = 1        (요구 6)
yaw_gain          = 0.0      yaw=0, 경로 접선 규약 (8/7 실측: 5배 효과)
curvature_gain    = 1.0      ← 다음 시험 대상
gps_style         = false
lateral_sign      = +1.0     방향 정상 확인됨
```

실차 확인(8/8): 초록점 (2.826,+0.301) → 송신 (2.825,+0.301), κ 0.0746,
검산 `2y/(x²+y²)` 일치. **명령은 의도대로 나간다.**

## ★ 다음에 할 실험 (아직 한 번도 안 됨)

`send_target_as_is=true` + `yaw_gain=0` 조건에서 **`curvature_gain` 스윕 (1 → 3 → 5 → 7)**.

- 8/7 에 돌린 curvature_gain 스윕은 `gps_style` 모드였는데 그 경로에는 `curv_gain`
  적용이 없어 **무효한 시험**이었다. 초록점 모드에서는 한 번도 안 해봤다.
- `curvature` 는 **위치가 아니라 제어 입력**이므로, 점 위치는 초록점 그대로 두고
  곡률만 보정하면 **6개 조건을 모두 지키면서** 조향을 키울 수 있다.
- 기하 요구 대비 실행률이 20% 였으므로 보정계수 약 5 가 필요할 것으로 본다.
  κ 0.169 × 5 = 0.845 로 물리 한계 0.870 안쪽이라 포화도 안 걸린다.
- **str 이 게인에 비례하면** → 계수 확정하고 종료. **안 변하면** → 곡률 필드로는
  불가능하므로 다른 경로를 찾아야 한다.

⚠ 8/8 측정은 **차량 배터리 없음**으로 무효다(v_ref 0.2 인데 v=0, str −13.83°가
명령 등가 2.54° 의 544% — 물리 응답일 수 없음). **배터리 연결 후 재측정할 것.**

## 지금까지 배제된 것 (재시도 불필요)

| 대상 | 결과 |
|---|---|
| 점 개수 1 vs 20 | 차이 없음. `CAN_Processor` 차트가 `n_points` 를 아예 안 읽음 |
| GPS 규약 완전 복제 (`gps_style`) | 초록점 1개와 결과 동일 (str −1.16°) |
| 속도 0.2 → 0.5 | 비율 27% 로 일정, 무관 |
| 송신점 x 위치 | 영향 미미 |
| `y` 크기 | 6.6배 줄여도 8.5→5.2° (약함) |
| dSPACE MPC 수식 | 정상. GPS 케이스는 모델과 일치 |

## 확정된 사실

- **`yaw` 가 조향을 5배 좌우한다** — κ 고정 상태에서 yaw −82.8°→0° 로 str 1.29°→6.8° (2회 재현)
- **dSPACE 조향은 `curvature` 필드가 지배하지 않는다** — GPS 위빙 run 은 송신 κ 등가
  3.26° 인데 실측 조향 ±28.6° 포화. 상관: `atan2(y,x)` −0.95 vs `curvature` −0.39
- **미리보기 = N_p × Ts × v_ref** (v=0.2 → 4cm). 회피 기하는 2~3m 스케일
- **정착에 수십 초 걸린다.** 스윕 dwell 은 최소 25~30초로 잡을 것 (12~15초는 정착 전 값)

## 하드웨어·환경 주의사항

| 항목 | 내용 |
|---|---|
| `can0` | 매번 수동으로 올려야 함: `sudo ip link set can0 up type can bitrate 1000000 restart-ms 100` (sudo 필요, 에이전트가 못 함) |
| 배터리 | 없으면 `v=0`, `str` 이 무효값. **측정 전 반드시 확인** |
| estop 동적 기준 | 스탠드에서 오탐·래치됨(`HAZARD_TRACK_MISSING_HOLD`). `ros2 param set` 으로 안 풀리고 **노드 재시작** 필요. 스탠드 시험은 `dynamic_enabled:=false` 로 |
| 라이다 | USB 재연결 시 드라이버가 죽은 포트를 잡고 있어 `/scan` 이 멈춤(`SCAN_TIMEOUT`). 드라이버 재시작 필요 |
| 불리언 파라미터 | `gps_style`·`send_target_as_is`·`scale_match` 는 콜백에 추가됨(라이브 변경 가능). 과거에 누락되어 모드 전환이 안 먹은 적 있음 |
| 좀비 프로세스 | `ros2 launch` 자식이 안 죽는 경우 있음. PID 로 `kill -9` 하고 `ros2 daemon stop` 으로 그래프 캐시 정리 |

## 대안 구성 (필요 시 즉시 전환, 전부 라이브 파라미터)

| 방식 | 설정 | 8/7 실측 str | 회피 성립 |
|---|---|---|---|
| ① 초록점 직송 | `send_target_as_is:=true` | −1.16° | ✗ 측방 0.15m |
| ② GPS 20점 | `gps_style:=true` | −1.16° | ✗ |
| ③ 당김+역산 | `send_target_as_is:=false` `scale_match:=true` `clear_before_m:=0.85` | **−6.89°** | ✓ 측방 0.89m |

③ 은 회피가 성립하지만 송신점이 초록점과 달라 요구 2·4 에 어긋난다.
**①에 `curvature_gain` 보정을 얹어 ③ 수준의 조향을 내는 것이 목표다.**
→ **이 목표는 H 에서 기각됐다.** `curvature_gain` 은 조향을 전혀 바꾸지 못한다.

---

# H. ★ curvature_gain 스윕 — 곡률 필드는 조향을 전혀 바꾸지 못한다 (2026-08-09)

bag: `~/avoid_logs/curvsweep_20260809_165640`
구성: `field_session.launch.py mode:=avoid v_ref:=0.2 dynamic:=false`,
`send_target_as_is=true` · `yaw_gain=0` · `gps_style=false` · `lateral_sign=+1` · `y_scale=1`

G 절이 지목한 "아직 한 번도 안 해본" 시험. 8/7 스윕은 `gps_style` 경로였는데 그 경로엔
`curv_gain` 적용이 없어 무효였다. 이번엔 초록점 직송 경로에서 돌렸고,
그 경로는 실제로 `curv_gain` 을 곱한다 (`avoid_to_ref.py` 의 `_rp(... s*kappa*self.curv_gain)`).

## 선결 조건 — 8/8 무효 원인 해소

| 항목 | 8/8 | 8/9 |
|---|---|---|
| 배터리 | 없음 (`v=0`, str 이 명령의 544%) | **정상 — `v=0.1999` (v_ref 0.2 추종)** |

8/8 측정을 통째로 버린 이유가 배터리였다. 이번엔 dSPACE 가 실제로 속도를 추종하므로
str 은 물리 응답이다.

## 포화 회피 — 콘을 멀리 옮겨 네 게인을 전부 한계 안에 넣었다

`curv_gain` 은 k_max 클램프 **뒤에** 곱해지므로 물리 한계를 넘는 κ 가 그대로 나간다.
콘이 가까우면 ×5·×7 이 한계를 넘어 "곡률이 안 먹혀서"인지 "그냥 한계라서"인지
구분이 안 된다. 그래서 앞범퍼~콘 1.55m → **2.60m** 로 옮겨 κ 를 0.196 → 0.109 로 낮췄다.

거리 레버가 유효한 이유: `detect_range_m=3.0` 은 **앞범퍼~콘(gap)** 에 걸리는데
κ 에 들어가는 건 **후축 원점 기준 `obs_x` = gap + `lidar_x`(0.76)** 다. 그 0.76m 차이 덕에
감지 상한을 안 건드리고도 κ 를 낮출 수 있다. 초록점의 y 는 자유롭지 않다 —
`clear = 차폭/2 + 여유 = 0.46m` 로 **콘 가장자리에서 고정**이라, 측방으로 옮겨도 y 는 안 변한다.
**전방거리만이 κ 를 낮추는 레버다.**

## 결과 — 통제 변수 완벽, 응답 0

각 구간 30s, 뒤쪽 절반(정착분)의 중앙값:

| gain | 송신 x | 송신 y | atan2(y,x) | 송신 κ | CAN 카운트 | **실측 str** |
|---|---|---|---|---|---|---|
| 1 | 3.365 | 0.642 | 10.80° | 0.1095 | 219 | **2.272°** |
| 3 | 3.365 | 0.642 | 10.81° | 0.3283 | 657 | **2.273°** |
| 5 | 3.365 | 0.642 | 10.81° | 0.5472 | 1094 | **2.272°** |
| 7 | 3.364 | 0.642 | 10.80° | 0.7665 | 1533 | **2.268°** |

**점의 기하(x·y·방향)는 네 구간 모두 동일하고 곡률만 7배 변했는데, str 은 0.005° 움직였다.**
명령 등가로는 3.72° → 24.46° (6.6배)를 요구했는데 실행은 그대로다. 실행률 61% → 9.3%.

## 이것이 계측 오류가 아닌 근거 3가지

1. **str 은 얼어붙지 않았다** — 같은 bag 에서 서로 다른 값 968개, 초반 60s 는
   −7.85°~−1.46° 로 크게 움직였다. 스윕 시작 후 200s 동안만 −2.27° 로 고정이다.
2. **CAN 이 자르지 않았다** — `quantize` 의 클램프는 ±32767 카운트인데 κ 0.7665 는
   1533 카운트다. dSPACE 는 7배 커진 곡률을 온전히 받았다.
3. **송신 메시지가 실제로 변했다** — bag 의 `/adas/target_ref` κ 가 0.1095→0.7665 로 기록됨.

## 결론

G 절의 판정 기준 "안 변하면 → 곡률 필드로는 불가능하므로 다른 경로를 찾아야 한다" 에
해당한다. **`curvature_gain` 은 조향 레버가 아니다. 이 방향은 종결한다.**

F-11 은 상관 −0.39 로 "곡률이 지배하지 않는다"까지 말했는데, 이번엔 다른 변수를 전부
고정한 통제 실험에서 **응답이 정확히 0** 임을 보였다. F-11 의 배제를 확정으로 승격한다.

## ★ 다음 레버 — 송신점을 초록점 방향 그대로 당기기

곡률이 아니라 **점의 기하**가 조향을 정한다(F-11: `atan2(y,x)` 상관 −0.95).
그런데 같은 각도라도 **거리**에 따라 조향이 크게 다르다는 증거가 있다:

| 출처 | 송신점 x | atan2(y,x) | 실측 str |
|---|---|---|---|
| F-11 y 스윕 | ≈0.80 | 12.4° | **6.21°** |
| H (이번) | 3.365 | 10.80° | **2.27°** |

거의 같은 각도인데 str 이 2.7배 차이다. **미리보기가 N_p×Ts×v_ref = 4cm** 뿐이라,
먼 점으로 가는 quintic 은 앞 4cm 가 거의 평평하고 가까운 점은 처음부터 휜다.

→ **초록점을 지나는 반직선 위에서 거리만 ~1m 로 당겨 보낼 것.**
방향 `atan2(y,x)` 를 보존하므로 "초록점 방향으로 조향"(요구 2·4)은 지키면서
크기만 키운다. 대안 ③ 과 달리 측방 y 를 유지한 채 x 만 줄이는 게 아니라
**방향을 유지한 채 거리를 줄인다** — 요구 2·4 와 충돌하지 않는 유일한 형태다.

예측: 초록점 (3.365, 0.642, 10.8°) → 당김점 (0.982, 0.187) 이면 F-11 의 6.21° 근방.
③ 이 낸 −6.89°(회피 성립, 측방 0.89m) 와 같은 급이다.

## 부수 수정 — RViz static TF 충돌 (같은 날)

`base_link → laser_frame` 이 **두 곳에서 다른 값으로** 발행되고 있었다:

| 발행자 | xyz | yaw |
|---|---|---|
| `ydlidar_launch.py` (드라이버 vendor launch) | (0, 0, 0.02) | 0° |
| `stack_avoid_node` (params.yaml 실측) | (0.76, 0, 0.065) | 90° |

같은 parent→child 에 static publisher 가 둘이면 tf2 버퍼는 나중 도착분을 잡으므로
어느 쪽이 이길지가 RViz 기동 타이밍마다 달라진다. placeholder 가 이기면 스캔이
**원점에 90° 틀어져** 그려져 마커와 안 맞는다. **표시만의 문제로, 감지·송신은 무관하다**
(`stack_avoid_node` 는 TF 가 아니라 params.yaml 로 자체 계산한다).

조치: 드라이버 워크스페이스는 stack_parking 과 공유하므로 건드리지 않고,
stack_avoid 의 launch 4개(`field_session`·`avoid_drive_full`·`avoid_estop_joint`·`avoid_hw_log`)가
vendor launch 를 include 하는 대신 **드라이버 노드만 직접** 띄우도록 바꿨다.
재시작 후 transform 1개만 남는 것을 확인했다.

---

# I. GPS candump 직접 파싱 → 규약 정정 + 방향 보존 20점 송신 (2026-08-09)

H 절이 지목한 "송신점 당기기"를 구현하면서, **GPS 규약을 전언이 아니라 raw CAN 으로
직접 검증**했다. `~/gps_bags/run1_20260803_182800`(직선)·`run1_20260806_192400`(S자)의
candump 를 0x100/0x101~0x114/0x202 로 디코딩했다. 결과 **이전 세션 메모 한 줄이 틀렸고**,
"1점이면 dSPACE 무반응"이라는 통설의 출처도 설명된다.

## I-1. ★ 정정 — "GPS 첫 점 x≈1.39m" 은 최댓값이었다

| 항목 | 8/3 직선 run | 8/6 S자 run |
|---|---|---|
| 사이클 수 (20점·주행중) | 10,180 | 8,258 |
| 첫 점 거리 **중앙** | **0.25 m** | **0.97 m** |
| 첫 점 거리 5~95% | 0.07 ~ 0.79 m | 0.81 ~ 1.15 m |
| 첫 점 거리 최대 | 0.786 m | **1.392 m** ← 이 값이 "규약"으로 잘못 기록돼 있었다 |
| 점 간격 중앙 | 0.314 m | 0.320 m |
| 첫 점 \|yaw\| 중앙 | 18.14° | 3.12° |
| 첫 점 \|κ\| 중앙 | 0.0165 | 0.0405 |
| 첫 점 y 5~95% | −0.41 ~ +0.70 m | −0.67 ~ +0.62 m |
| 실측 str 범위 | −28.13 ~ +28.60° | −27.92 ~ +27.74° |

**GPS 의 첫 점은 사실상 차량 바로 앞(0.1~1.1m)에 있고, 그 y 가 경로 이탈량이다.**
κ 는 0.02~0.04 로 거의 0 인데 조향은 ±28° 까지 나온다 — H 절의 "곡률은 레버가 아니다"가
GPS 쪽 데이터에서도 재확인된다. 조향을 만든 것은 **근거리 점의 위치**뿐이다.

우리는 3.365m 를 보내고 있었다. 규약 위반의 크기가 기록돼 있던 것보다 컸다.

## I-2. "1점이면 무반응" 의 출처 — 종점 정지와 교락돼 있다

두 bag 모두 n_points 가 20 에서 내려가는 구간이 있다:

| 구간 | 8/3 | 8/6 |
|---|---|---|
| n_points=20 | 10,921 사이클 | 8,599 |
| n_points 3~19 | 2,120 | 1,120 |
| n_points 1~2 | 582 | 711 |

n_points≤2 구간에서 조향 변동률이 10.1%·0.3% 로 죽는 것은 사실이다. 그런데 **그 구간은
두 run 모두 트랙 맨 끝 하나뿐**이다 (8/3: 130.4~136.2s / 136s, 8/6: 97.2~104.3s / 104s).
같은 구간의 v_ref 평균이 0.005·0.152 m/s — `GpsPath.at_end` 종점 정지 래치가 걸려
**차가 서고 있는 구간**이다. v_ref→0 이면 F-7 대로 `B_k=0` 으로 추종항이 소멸한다.

→ **"점 개수" 와 "정지" 가 완전히 교락돼 있어, GPS bag 으로는 점 개수의 효과를 분리할 수
없다.** 무반응의 원인이 점 개수라는 근거도, 아니라는 근거도 이 데이터에는 없다.

## I-3. 어느 점이 조향을 만드는가 — 상관은 index 0 에서 최대

20점 주행 구간만 골라, 각 index 의 `atan2(y,x)` 와 0.3s 뒤 str 의 상관을 봤다:

| 점 index | 0 | 1 | 2 | 3 | 4 | 6 | 9 | 14 | 19 |
|---|---|---|---|---|---|---|---|---|---|
| 8/3 상관 | **−0.799** | −0.697 | −0.598 | −0.522 | −0.465 | −0.379 | −0.299 | −0.245 | −0.209 |
| 8/6 상관 | **−0.957** | −0.919 | −0.875 | −0.828 | −0.781 | −0.689 | −0.566 | −0.392 | −0.253 |

**두 run 모두 index 0 에서 최대이고 단조 감소한다.** 20점이 다 같은 경로 위라 서로
상관돼 있으니 뒤 점들의 값도 0 은 아니지만, 먼 점이 지배한다는 신호는 없다.
F-7 의 모델 배선(`Bus Selector1 out6~9 = REF_POINT_00 → MPC in:2`)과 일치한다.

## I-4. 그래도 20점으로 보낸다 (이기돈 지시)

I-2 가 보여주듯 **"1점이라 무반응"을 배제할 데이터가 없다.** 20점은 비용이 없고
(주기당 21프레임, 1Mbps 기준 여유), GPS 와 비트 단위로 같은 형태가 된다.
그리고 우리 쪽에서 보내면 GPS bag 이 못 준 **깨끗한 비교**가 가능하다 —
점 0 의 기하를 완전히 고정한 채 `ray_n_points` 만 1↔20 으로 바꾸면 되고,
GPS 와 달리 v_ref 는 두 구간 모두 0.2 로 유지된다(정지와 교락되지 않는다).

## I-5. 구현 — 방향 보존 반직선 위 20점

```
th = atan2(y_초록점, x_초록점)                    # ★ 보존
L_i = clamp(min(ref_lookahead, d), ref_x_min) + i × ray_spacing
점_i = (L_i·cos th, L_i·sin th),  yaw = th × yaw_gain,  κ_i = 2y_i/(x_i²+y_i²)
```

기본값: `ref_lookahead_m 0.90` (I-1 의 GPS 대역) · `ray_n_points 20` · `ray_spacing_m 0.32`.

**등곡률 호가 아니라 직선(반직선)인 이유** — GPS 8/3 직선 run 이 정확히 이 형태다:
점들이 차량 기준 옆으로 벗어나 있고(첫 점 y = 이탈량 ±0.7m), κ≈0, yaw=경로 접선.
회피 목표점까지의 호를 샘플하면(`gps_style`) 첫 점 y 가 0.005m 로 뭉개져
**회피 의도가 근거리 점에서 사라진다** — gps_style 이 초록점 직송과 같은 결과를 낸 이유로
가장 유력하다(그 8/7 비교 자체는 파라미터 콜백 누락으로 무효였다).

대안 ③(당김+역산)은 y 를 유지하고 x 만 줄여 **방향이 변한다**. 반직선 방식은 방향을
보존하므로 요구 2·4 와 충돌하지 않는다.

측방 y 는 0.642 → 0.169 로 작아지지만, 점은 매 스캔 갱신되므로 접근할수록 같은 방향의
점이 계속 나와 측방 이동이 누적된다(GPS·pure-pursuit 와 같은 거동).

## I-6. 실제 노드 출력 검증 (headless, `/perception/avoid` 주입 → `/adas/target_ref`)

초록점 (3.365, +0.642), 방향 10.80° 입력 → **n_points = 20**:

| i | x | y | 거리 | 방향 | yaw | κ |
|---|---|---|---|---|---|---|
| 0 | 0.884 | +0.169 | 0.900 | **10.80°** | 0.000 | +0.416 |
| 1 | 1.198 | +0.229 | 1.220 | **10.80°** | 0.000 | +0.307 |
| 3 | 1.827 | +0.349 | 1.860 | **10.80°** | 0.000 | +0.202 |
| 19 | 6.856 | +1.308 | 6.980 | **10.80°** | 0.000 | +0.054 |

점 간격 0.320~0.320m, **전 20점 방향각 편차 0.0000°**. 부호 반전(초록점 y<0)도 대칭 확인.
`ray_n_points:=1` 이면 점 0 하나만 나간다 — 기하 동일, 개수만 다름.

## I-7. ★ 다음 실측 — 3구간 비교, H 절과 같은 배치

> **현장 절차서: `RUNBOOK_20260809_nsweep.md`** — 터미널별 복붙 명령·산출물 경로·
> 트러블슈팅까지. 아래는 그 시험의 설계 근거와 판정 기준이다.

콘 위치가 바뀌면 초록점 방향각이 달라져 비교가 무효다. **앞범퍼~콘 2.60m 유지.**
구간당 dwell 최소 25~30초(F-9: 12~15초는 정착 전 값).

| # | 구성 | 송신 점 0 | 예측 str | 무엇을 가리는가 |
|---|---|---|---|---|
| ⓐ | `ray_pull:=false send_target_as_is:=true` | (3.365, 0.642) | 2.27° | H 재현 = 기준선 |
| ⓑ | `ray_pull:=true ray_n_points:=1` | (0.884, 0.169) | ? | **거리** 효과 단독 |
| ⓒ | `ray_pull:=true ray_n_points:=20` | (0.884, 0.169) | ? | **점 개수** 효과 단독 |

- ⓑ > ⓐ → 거리가 레버다 (F-11 의 0.80m/12.4° → 6.21° 와 정합).
- ⓒ > ⓑ → **점 개수가 실제로 영향을 준다** = I-3 의 상관 분석과 F-7 의 모델 배선이 둘 다
  틀렸다는 뜻이다. 이 경우 손상민에게 모델 재확인을 요청할 것 (Bus Selector 가
  REF_POINT_00 만 뽑는데 왜 개수가 영향을 주는지 — 예: 헤더 latch 조건, 버퍼 완성 판정).
- ⓑ≈ⓒ≈ⓐ (전부 2.3°대) → PC 측 레버 전부 소진. dSPACE 구조 문제로 확정하고 이관.

```bash
ros2 launch stack_avoid field_session.launch.py mode:=avoid v_ref:=0.2 dynamic:=false
ros2 param set /avoid_to_ref ray_pull false                     # ⓐ
ros2 param set /avoid_to_ref ray_pull true
ros2 param set /avoid_to_ref ray_n_points 1                     # ⓑ
ros2 param set /avoid_to_ref ray_n_points 20                    # ⓒ
ros2 param set /avoid_to_ref ref_lookahead_m 0.5                # 이후 거리 스윕
```

★ ⓒ 는 CAN 이 주기당 2 → 21 프레임이 된다. 버스 부하 7%→ 약 30% (1Mbps).
드롭·에러가 나면 `can_bridge_node` 로그와 `ip -s link show can0` 을 함께 볼 것.

## I-8. 손상민에게 넘길 것 (dSPACE 영역)

1. **미리보기 길이가 속도에 비례한다.** `pointSpacing = Ts × |v_ref|`,
   `requestedArc = (0:N_p) × pointSpacing` → v=0.2 에서 지평 전체가 4cm.
   저속 주행하는 모든 스택이 같은 조건이다. 고정 길이(최소 1~2m)로 두거나
   `pointSpacing` 을 v_ref 에서 분리해야 하는 것 아닌지.
2. **`curvature` 필드가 조향에 반영되지 않는다** (H: 7배 변화 → str 0.005°).
   GPS 쪽 데이터도 같은 방향이다 — κ 중앙 0.02~0.04 로 거의 0 인데 str ±28°.
   미사용이면 PROTOCOL.md 에 "송신하되 미사용"으로 명시해야 한다.
3. **REF_POINT_01~19 를 쓰는가?** 모델 배선상 `REF_POINT_00` 만 MPC 로 들어가는데,
   GPS 는 20점을 보내고 있다. 쓰지 않는다면 GPS 도 1점으로 줄일 수 있고, 쓴다면
   PROTOCOL.md §"모든 소스 1점" 합의를 고쳐야 한다.

## I-9. 부수 수정 — `gps_style` 경로가 estop 게이트를 건너뛰고 있었다

`tick()` 의 `gps_style` 분기가 ref 점을 채운 뒤 **early return** 해서 `self.gate.block()`
을 지나치지 않았다. 그 모드에서는 estop 이 인가돼도 `v_ref` 가 0 으로 덮이지 않는다
(진단 전용 모드라 실주행에 쓰인 적은 없다). early return 을 없애 모든 경로가 게이트를
통과하도록 고쳤고, estop 미수신 상태에서 `v_ref=0`·`n_points=20` 나오는 것을 확인했다.


---

# J. ★ 점 개수·거리 스윕 — PC 측 레버 소진 (2026-08-09 저녁)

bag: `~/avoid_logs/nsweep_20260809` (8분 50초, 구간 라벨 21개)
CAN: `~/avoid_logs/nsweep_20260809_can.log` · 파라미터: `..._params.yaml`
구성: 스탠드, 앞범퍼~콘 2.60m, `field_session mode:=avoid v_ref:=0.2 dynamic:=false`

## J-0. 측정 유효성 — 전부 통과

| 항목 | 실측 | 판정 |
|---|---|---|
| 송신 v_ref | 중앙 0.200, 0인 비율 **0.6%** | estop 개입 무시 가능 |
| dSPACE v | 중앙 **0.1999** (0~0.2044) | **배터리 정상** — 8/8 무효 원인 해소 |
| state | AVOID 52,718 / LANE 332 | 회피 상태 유지 |
| n_points | 명령대로 1·2·3·5·8·12·16·20 분포 | 스윕이 실제로 걸렸다 |
| str 고유값 | 구간당 196~236개 | 액추에이션 생존 (얼어붙지 않음) |

## J-1. ★ 점 개수 — 응답 정확히 0

각 구간 30초, 뒤쪽 절반(정착분) 중앙값:

| n_points | 점0 x | 점0 y | 방향° | 송신 κ | **실측 str** |
|---|---|---|---|---|---|
| 1 | 0.883 | +0.172 | 11.01 | +0.424 | **−2.91°** |
| 2 | 0.883 | +0.172 | 11.00 | +0.424 | **−2.91°** |
| 3 | 0.883 | +0.172 | 10.99 | +0.424 | **−2.91°** |
| 5 | 0.883 | +0.172 | 10.99 | +0.424 | **−2.91°** |
| 8 | 0.883 | +0.172 | 10.99 | +0.424 | **−2.91°** |
| 12 | 0.883 | +0.172 | 10.99 | +0.424 | **−2.91°** |
| 16 | 0.883 | +0.172 | 11.00 | +0.424 | **−2.91°** |
| 20 | 0.883 | +0.172 | 10.99 | +0.424 | **−2.91°** |
| 1 (브래킷) | 0.883 | +0.172 | 10.99 | +0.424 | **−2.91°** |

**점 0 의 기하가 완전히 고정된 채 개수만 1→20 으로 20배 변했는데 str 은 소수점
둘째 자리까지 동일하다.** 마지막 브래킷도 첫 구간과 같아 세션 드리프트도 없다.

→ **점 개수는 조향 레버가 아니다. 확정.** I-2 가 "GPS bag 으로는 분리 불가"라고 한
질문에 이 시험이 답을 냈다. GPS 의 n_points≤2 무반응 구간은 점 개수가 아니라
**트랙 종점 정지(v_ref→0)** 때문이었다. F-7 모델 배선(REF_POINT_00 만 MPC 로)과
I-3 상관 분석(index 0 최대)이 모두 지지된다.

**PROTOCOL.md §"모든 소스 1점" 합의를 바꿀 이유가 없다.** 20점 송신은 CAN 부하만
7%→32% 로 올릴 뿐 얻는 것이 없다. 회피는 1점으로 되돌려도 된다.

## J-2. 거리 — 듣긴 하지만 필요량에 한참 못 미친다

| 송신점 거리 | 점0 x | 방향° | 송신 κ | **실측 str** | 명령 등가 | 실행률 |
|---|---|---|---|---|---|---|
| 0.4 m | 0.785 | 11.00 | +0.477 | **−3.32°** | +15.84° | 21.0% |
| 0.9 m | 0.883 | 10.99 | +0.424 | **−3.32°** | +14.15° | 23.5% |
| 1.4 m | 1.374 | 10.99 | +0.272 | **−3.29°** | +9.20° | 35.7% |
| 2.0 m | 1.963 | 10.99 | +0.191 | **−2.98°** | +6.47° | 46.1% |
| 3.29 m (초록점 직송, 3회) | 3.289 | 10.97 | +0.114 | **−2.35°** | +3.87° | 60.7% |

- 방향각은 전 구간 11.0° 로 보존됐다 — 설계대로다.
- **3.29m → 0.9m 로 당겨 얻은 것은 2.35° → 3.32°, 즉 +41% 뿐이다.**
- **0.9m 아래로는 포화** — 0.4m 와 0.9m 가 같다. 더 당길 곳이 없다.

## J-3. 예측 실패 — I-7 의 6~9° 는 재현되지 않았다

I-7 은 F-11(송신 0.80m·12.4° → str 6.21°)을 근거로 6~9° 를 예측했다. 실측은 3.32° 다.
F-11 값은 다른 세션·다른 구성에서 나온 것이라 이번 통제 조건에서 재현되지 않았다.
**회피 성립에 필요한 것은 ③(당김+역산)이 낸 6.89° 급인데, 방향을 보존하는 한
도달할 수 없다는 것이 이번 스윕의 결론이다.**

## J-4. 결론 — PC 측 레버 소진

★ 아래 "조향 변화" 열은 **그 레버를 흔들었을 때 실측 str 이 얼마나 변했나**다.
   보낸 값이 0이라는 뜻이 아니다 — 명령은 항상 충분히 컸다(예: κ +0.424 = 등가 14.15°,
   점 개수는 최소 1개). 조향도 0이 아니다(−2.91°, 구간당 고유값 200개 이상 = 살아 있음).
   문제는 **명령의 20% 만 실행된다**는 것이고, 명령을 키워도 그 20% 가 안 늘어난다.

| 레버 | 흔든 범위 | 명령 등가 조향 | **실측 str 변화** |
|---|---|---|---|
| curvature (H) | 0.109 → 0.767 (7배) | 3.72° → 24.46° | 2.272° → 2.268° = **없음** |
| 점 개수 (J-1) | 1 → 20 (20배) | 14.15° 고정 | −2.91° → −2.91° = **없음** |
| 송신점 거리 (J-2) | 3.29 → 0.4 m | 3.87° → 15.84° | −2.35° → −3.32° = **+41%**, 0.9m 포화 |
| yaw (F-11) | 82.8° → 0 | — | 1.29° → 6.8° = 5배 (이미 반영, yaw_gain=0) |

방향을 보존하면서 PC 가 바꿀 수 있는 것은 전부 흔들었다. 최대 3.32° 이고
회피에 필요한 것은 그 2배다. ~~남은 것은 dSPACE 측 구조뿐이다~~
→ **K 절에서 수정: "구조 문제"가 아니라 게인이 v 에 비례하는 것. v_ref 0.2 가 원인.**

우선순위: ①미리보기 = N_p×Ts×v_ref (v0.2 → 4cm) ②제어 벌점 R 대비 추종항 1e-5
— 둘 다 F-7 에서 모델 소스로 확인된 것이고, 이번 스윕은 "PC 에서는 못 고친다"를
실측으로 못 박았다.

## J-5. 미해결 — 조향 부호가 명령과 반대다

명령 κ 가 **+0.424(좌)** 인데 실측 str 은 **−2.91°** 로, 전 구간 예외 없이 반대 부호다.
H(8/9 낮)도 같았다. 반면 F-11 은 "명령과 같은 부호로 돌아온다(κ +0.25 → str +5.77°)"
라고 적혀 있어 서로 어긋난다.

**지상 회피 주행 전에 반드시 눈으로 확인할 것** — 스탠드에서 콘이 왼쪽에 있을 때
바퀴가 실제로 어느 쪽으로 도는지. 반대로 돌면 장애물로 조향하는 것이 된다.
텔레메트리만으로는 판정할 수 없다(§F-8: 반전 지점이 str 보고 지점보다 하류).

## J-6. 부수 — dSPACE watchdog 부재 확인 및 자동 0 복귀 구현

시험 전 점검에서 **PC 송신이 전무한데 dSPACE 가 `v=0.1988` 을 유지**하고 있었다
(전날 스윕의 v_ref 0.2). CLAUDE.md §3 의 "헤더 counter 30ms 미갱신 → v_ref=0" 이
동작하지 않는다. `v_ref=0` 을 보내자 즉시 0 으로 떨어졌고, 송신을 끊은 뒤에도 0 을
유지했다 — 즉 dSPACE 의 동작은 watchdog 이 아니라 **"마지막 값 무기한 유지"** 다.

대응: `stack_avoid/can_zero.py` 가드를 만들어 주행 launch 3개
(`field_session`·`avoid_drive_full`·`avoid_estop_joint`)에 넣었다. launch 를 `Ctrl-C`
하면 SocketCAN 에 직접 `v_ref=0 · ref_point 0` 을 30회 쓰고 종료한다.
브리지가 먼저 죽어도 동작한다. 실제 launch 종료로 검증했다:

```
[can_zero-1] can_zero: can0 에 v_ref=0 · ref_point 0 을 30회 송신 완료
버스 확인: 0x100 30개 (state 0, n_points 1, v_ref 0mm/s) + 0x101 30개 (전부 0)
```

★ `ros2 run` 으로 감싸면 래퍼가 SIGINT 를 삼켜 가드가 돌지 않는다 — launch 에서는
반드시 `Node` 액션으로 띄울 것(실측으로 확인).

**이건 회피 이슈가 아니라 시스템 안전 이슈다.** PC 가 죽어도 차가 마지막 속도로
계속 간다는 뜻이므로 §7 판정 기준("PC 사망 시 정지 개시까지의 이동거리")의 전제가
성립하지 않는다. 손상민에게 최우선으로 넘길 것.

## J-7. 부수 — CAN 자동 설정 설치

`70-can-auto.rules` + `can_up.sh` 가 미설치 상태였다(PCAN 재삽입 시 수동 up 필요).
설치하고 udev `add` 이벤트로 검증했다 — `state UP · bitrate 1000000 · restart-ms 100`,
시스템 로그 `can_up[…]: can0 up @ 1Mbps`. 이제 꽂기만 하면 자동 활성화된다.

---

# K. ★ GPS 대조 재분석 — "dSPACE 구조 문제"가 아니라 **속도 비례 게인**이었다 (2026-08-09 밤)

이기돈 반박: "GPS 는 같은 dSPACE 로 정상 주행하는데 무슨 dSPACE 문제냐."
→ 정당한 반박이고, GPS raw CAN 을 다시 파싱해 검증한 결과 **J-4 의 결론을 수정한다.**

## K-1. GPS 전달 곡선 — 점0 방향각 → 실측 str (8/6 S자 run, 0.3s 지연 보정)

| 점0 \|방향\| | 사이클 | \|str\| 중앙 | **게인** |
|---|---|---|---|
| 1.6° | 3,730 | 1.98° | 1.25 |
| 4.0° | 1,688 | 3.59° | 0.90 |
| 7.8° | 590 | 6.55° | 0.84 |
| **12.3°** | 210 | **10.31°** | **0.84** |
| 17.2° | 230 | 14.70° | 0.85 |
| 22.6° | 550 | 15.35° | 0.68 |
| 40.3° | 710 | 24.63° | 0.61 (물리 포화 27.3° 접근) |

GPS 는 점0 방향각의 **84%** 를 조향으로 실행한다. 우리 스탠드 시험(J)은 같은 11° 에서
2.91° = **26%**. 같은 dSPACE 인데 3.5배 차이 — 여기까지가 반박의 근거다.

## K-2. ★ 결정적 — 같은 GPS 데이터를 실측 속도로 조건부 분해하면

| 점0 \|방향\| | 실측 v | 사이클 | \|str\| 중앙 | **게인** |
|---|---|---|---|---|
| 3~10° | 0.05~0.30 | 52 | 0.81° | **0.24** |
| 3~10° | 0.30~0.45 | 40 | 1.27° | **0.22** |
| 3~10° | 0.45~0.60 | 2,140 | 4.30° | **0.94** |
| 10~20° | 0.45~0.60 | 440 | 10.39° | 0.69 |

**GPS 도 저속에서는 게인 0.22~0.24 다.** 우리 스탠드 실측 0.26(v=0.2, 30초 정착)과
일치한다. GPS 가 "정상 주행"으로 보인 것은 파이프라인이 달라서가 아니라
**v_ref 0.5 로 달리기 때문**이다. (저속 표본 92개는 가감속 램프 순간이라 수는 적지만,
우리의 독립적인 정착 실측 0.26 과 F-7 의 수식이 모두 같은 값을 가리킨다.)

메커니즘은 F-7 에 이미 있었다:
- 미리보기 = N_p×Ts×**v** (0.2 → 4cm, 0.5 → 10cm)
- 제어 권한 B_k = **v**·Ts/L → 추종항 ∝ **v²** (0.2 대비 0.5 는 6.25배)

## K-3. 그동안의 실패가 전부 설명된다

| 시험 | v_ref | 결과 | 재해석 |
|---|---|---|---|
| F-6 dummy_ref κ±0.5 (스탠드) | 0.2 | ±1.8° | 게인 캡 안 |
| H curvature ×7 (스탠드) | 0.2 | 변화 0 | 게인 캡 안에서 곡률만 키움 |
| J-1 점 개수 ×20 (스탠드) | 0.2 | 변화 0 | 〃 |
| J-2 거리 3.29→0.4m (스탠드) | 0.2 | +41% 후 포화 | 방향각 게인은 살아 있으나 캡이 낮음 |
| GPS 8/6 (지상) | **0.5** | 게인 0.84~0.94 | 캡 밖 |

**모든 회피 시험이 v_ref 0.2 에서 돌았다.** 0.2 는 게인 0.25 캡 아래라 무엇을 바꿔도
3° 근처를 못 벗어난다 — 곡률·점 개수·거리 스윕이 전부 평평했던 이유다.

또한 **예전 "지상 회피 주행" bag 은 전부 무효였다** — ground_141208·153020·153637·
avoiddrive_20260806 모두 `v` 중앙 0.000 (estop 정지 또는 배터리 문제). 게다가 당시
파이프라인은 점0 을 (0.40, 0.028) = 방향 4.7° 로 보냈다. **유효한 지상 회피 주행은
아직 한 번도 없었다.**

## K-4. 부호 문제(J-5) 해소

GPS 에서 방향+ 일 때 str− 인 비율이 의미 있는 각도에서 **81~100%** 다. 그런데 GPS 는
경로를 정상 추종한다(8/6 S자 완주). 즉 **텔레메트리 str 의 부호가 명령 규약과 반대인
것이 정상 동작**이며(F-7 의 이중 부호 반전과 정합), 우리의 κ+ → str− 관측은 GPS 와
같은 건강한 패턴이다. F-11 의 "같은 부호로 회신" 기록이 오류였다.
지상 첫 주행 때 눈 확인은 여전히 할 것 (비용 0).

## K-5. 수정된 결론

> **회피 파이프라인은 GPS 와 동일하게 동작하고 있다. 문제는 회피만 v_ref 0.2 로
> 시험한 것이다. dSPACE 조향 게인이 속도에 비례해(설계 구조), 0.2 에서는 명령의
> 1/4 만 실행된다. GPS 는 0.5 로 달려서 정상으로 보였을 뿐이다.**

- 손상민 이관 항목 재구성: "조향이 고장"이 아니라 **"게인이 v 에 비례하는 설계"**.
  저속 미션(주차 등)이 있다면 미리보기·가중치의 속도 불변화가 필요하다는 제안으로.
  watchdog 부재(J-6)는 그대로 최우선.
- 회피의 즉시 경로: **속도를 GPS 대역(0.4~0.5)으로 올려서 시험한다.**

## K-6. ★ 다음 시험 2단계

**① 스탠드 속도 스윕 (즉시 가능, 5분)** — 콘·방향각 고정(11°), 속도만:

```bash
python3 src/stack_avoid/tools/gain_sweep.py \
    --param target_speed_mps --values 0.2 0.3 0.4 0.5 0.2 --dwell 30
```

예측: K-2 가 맞다면 str 2.9° → 0.45 부근에서 **8~10°** 로 뛴다.
0.5 에서도 3° 근처면 속도 가설 기각 → 남는 변수는 지상/스탠드 차이뿐.

**② 지상 회피 주행 (①이 확인되면)** — `v_ref:=0.45` 로 ⓐ 시나리오.
- ★ 사전에 박찬미와 estop 정지거리 재확인 필수 — `estop_on_distance_m 0.70` 은
  저속 기준. 0.45 m/s 의 제동거리·steer_lag(0.35s×0.45=0.16m)를 부등식에 반영할 것.
- 회피 기하도 재계산: 속도가 오르면 같은 거리에서 조향 시간이 준다. clear_before
  여유를 넉넉히.

---

# L. ★ 속도 스윕 — K 가설 확정. v=0.5 에서 명령의 98.5% 실행 (2026-08-09 밤)

bag: `~/avoid_logs/nsweep_20260809_b` · 구성: J 와 동일 (스탠드, 콘 2.60m, ray_pull 0.9m×20점)
콘·방향각(11.0°)·κ(+0.424, 등가 14.15°) **완전 고정, 속도만** 0.2→0.5→0.2.

| v_ref | 실측 v | **실측 str** | 실행률 | 고유값 |
|---|---|---|---|---|
| 0.2 | 0.2001 | **−2.66°** | 18.8% | 258 |
| 0.3 | 0.2987 | **−6.89°** | 48.6% | 483 |
| 0.4 | 0.3985 | **−10.30°** | 72.8% | 267 |
| 0.5 | 0.4992 | **−13.93°** | **98.5%** | 260 |
| 0.2 (브래킷) | 0.2003 | −4.01° | 28.3% | 219 |

- 속도 추종 전 구간 정상(±0.002) — 측정 유효.
- **str 이 속도에 거의 선형으로 비례**하고, 0.5 에서는 명령 등가(14.15°)를 사실상
  100% 실행한다. K-2 의 GPS 조건부 분석(저속 0.22~0.24 / 고속 0.94)과 정합.
- 예측(8~10°)보다 강함 — GPS 20~45° 대역의 게인 저하는 물리 포화 접근 때문이었고,
  11° 대역은 포화가 멀어 실행률이 1.0 까지 올라간다.
- 브래킷(0.2 복귀)이 −2.66 이 아니라 −4.01 — 내려오는 정착이 느리다(30s 부족).
  게인 서열에는 영향 없음.

## 결론

> **회피 조향 문제의 원인은 v_ref=0.2 하나였다. 0.5 로 올리면 dSPACE 는 우리가
> 보내는 회피 명령을 그대로 실행한다. 파이프라인(인지→ray_pull→CAN→MPC)은
> 처음부터 정상이었다.**

수치로: 회피 성립에 필요한 조향은 ~7°(F-10 의 ③ 기준). v=0.3 에서 이미 6.89°,
v=0.4 면 10.3° 로 여유 있다. **지상 회피 주행은 v_ref 0.4 를 기본으로 한다**
(0.5 는 실행률 여유가 없고 estop·제동 마진도 줄어든다).

## 다음 = 지상 회피 주행 (마지막 단계)

1. estop 거리 0.70 은 **그대로 두고 시험한다** (이기돈 결정, 2026-08-09).
   근거: v=0.4 정지거리 ≈0.30m < 0.70m (여유 2배) — 안전측은 이미 성립.
   오탐(회피 중 정지)은 페일세이프 방향이고, ⓐ 시나리오가 정확히 그 판정이다.
   시험 후 결과를 박찬미와 공유 — 오탐 시 값 조정은 그때 합의 (찬미 파라미터).
2. 통제 공간, `field_session mode:=avoid v_ref:=0.4 dynamic:=true` (지상은 동적 기준 복원).
3. 첫 진입 순간 바퀴가 초록점 쪽으로 꺾이는지 눈 확인 (K-4 의 부호 검증, 비용 0).
4. 성공 기준: 콘 측방 통과 시 차폭/2+여유(0.46m) 이상 이격, estop 미발동.

## 손상민 공유 (이관 아님 — 정보)

- 조향 게인이 v 에 비례하는 것은 모델 구조(미리보기 = N_p×Ts×v, B_k ∝ v)다.
  **0.4 이상에서는 문제없음을 실측 확인.** 저속 정밀 기동(주차)이 필요한 스택만
  영향 — stack_parking 에 전달 요망.
- watchdog 부재(J-6)는 별개로 여전히 최우선.

## L-보론. 정정 — 8/3 GPS 위빙의 원인 귀속 (이기돈 지적, 2026-08-09)

K 절 논의 과정에서 "8/3 위빙 = 저속 게인 때문"이라고 해석한 것은 **오류**다.
팀 이력: 위빙은 **lookahead 도입으로 해결**됐고, 속도 인상은 그 뒤였다.
8/3 bag 파싱도 이를 지지한다 — 점0 x 중앙 −0.022m(최근접점, lookahead 이전),
방향각 중앙 67.8° → ref 자체가 널뛰어 조향 포화 진동. 게인 문제가 아니라 ref 생성 문제.

속도-게인 결론(L)은 이 해석과 무관하게 성립한다 — 근거는 GPS 해석이 아니라
우리 차의 통제 실험(기하 고정·속도만 변경)이다. 두 사실의 양립:
- lookahead 이후 0.3 에서 GPS 정상 = 경로 추종은 필요 조향이 작고 이탈-보정
  루프가 게인 부족을 흡수한다.
- 회피는 0.3 에서 한계 = 11° 명령에서 7° 를 제때 뽑아야 하는데 실행분 6.89°.
  **0.4 권장 유지.**
