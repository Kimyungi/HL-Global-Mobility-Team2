# 회피 통합 실차 시험 런북 (차선+GPS+회피)

**launch: `adas_mgm/launch/REAL_VEHICLE_lane_gps_can.launch.py`** — 2026-08-12 통합된
avoid 스테이트의 **첫 실차 검증** 절차. 기본 주행 절차는 `RUNBOOK_lane_gps.md`를 그대로
따르고, 이 문서는 회피 시험에 **추가되는 것**만 담는다.

> 전제: 통합 코드(2026-08-12)가 빌드된 상태. avoidable 판정·maneuver_done 클리어런스·
> v_suggest 계수는 sim 검증만 된 **초기값**이다 — 이 시험이 그 값을 확정한다.
> **이기돈 동석 권장** (stack_avoid 판정 계수의 현장 확인).

**기대 동작 (CLAUDE.md §4):** 주행 중 전방 장애물 감지 + 회피 성립 → `AVOID(회피)` 전이
→ 회피 목표점 추종으로 비켜 감 → 통과 완료(maneuver_done) → **WAYPOINT 복귀**(GPS 트랙
재합류) → 차선 신뢰도 회복 시 LANE 재전이.

```
터미널 5개: B1(베이스) + V1(RTCM 중계) + V2(통합 launch) + M(state 모니터) + V3(go)
```

| 이름 | 어디서 | 역할 | 비고 |
|---|---|---|---|
| B1·V1·V2·V3 | — | RUNBOOK_lane_gps.md와 동일 | V2 인자만 §2 참조 |
| **M** | 차량 PC | `ros2 run adas_mgm state` | **시험 내내 켜 둔다** — 전이 이력이 곧 시험 기록 |

---

## 0. 콘(장애물) 배치 — 기하가 절반이다

| 항목 | 값 | 근거 |
|---|---|---|
| 장애물 | 콘 1개 (라이다 스캔 높이 6.5cm에 걸리는 물체) | lidar_mount.z_m |
| 위치 (종방향) | **트랙 중간 지점** | 출발 과도·종점 래치 구간 회피 |
| 위치 (횡방향) | **경로 정중앙 ±0.2m 이내** | 감지 통로 반폭 0.46m (차폭/2+측방여유) — 벗어나면 장애물로 안 봄 |
| 콘 양옆 열림 | **각 1.2m 이상** (벽·콘 없음) | 회피 오프셋 상한 1.0m + 여유 |
| 예상 AVOID 진입 | 콘 앞 ~3.0m | avoid.detect_range_m |
| 예상 통과 이격 | 콘 옆 ~0.55m | 콘반경0.1+차폭/2+lateral_margin 0.15 |
| 예상 복귀(done) | 감지 소실 후 **6초 내외** | (마지막 감지거리+전장 0.85+clear_margin 0.3)÷v. 3m에서 감지→2.4m쯤 소실이면 3.55m÷0.6 ≈ 5.9s (2026-08-15 v 0.5→0.6). 실제 계산은 dSPACE 실측 속도를 쓰므로 자동 추종 |

**안전 바닥 (회피가 실패해도):** stack_estop 정적 0.7m / 동적 1.2m 정지 + MGM TTC<0.8s
즉시 정지. 운전자는 **물리 비상정지에 손 올리고** 대기 (field_session 관례).
소프트웨어 정지 = **V2 Ctrl-C** — 종료 시 can_zero가 dSPACE 목표값 0을 송신한다 (2026-08-12 가드).

## 1. 정지 인지 확인 — 차 출발 전 (2분, 필수 게이트)

V2까지 띄운 상태(`wait_go` 정지 대기 — go는 아직)에서 콘을 차 전방 2~3m 경로 위에 놓고:

```bash
ros2 topic echo /perception/avoid --once
```

| 확인 | 기대값 |
|---|---|
| obstacle_detected | true |
| avoidable | **true** ← 이게 false면 주행 시작 금지 |
| ttc | 3~5 (거리÷0.6) |
| points | 1개, y 부호 = 열린 쪽 |
| v_suggest | **0.6** (= 목표속도 `target_speed_mps`. 회피 중 감속 금지 — 조향 하한 0.5 m/s, 이기돈 실측. 2026-08-15에 0.5→0.6: 0.5는 달성 속도가 중앙값 0.494로 하한 아래에 깔렸다) |

콘을 좌/우로 옮기면 `points[0].y` 부호가 따라 바뀌는지, 치우면 detected=false로 돌아오는지 확인.
avoidable이 안 서면 → 콘이 통로(±0.46m) 밖이거나 ttc<1.5 (§5 표).

## 2. 회피 주행 (차선 포함 full 통합)

차선·GPS·회피를 모두 켜고 **lane ↔ waypoint ↔ avoid** 전이를 함께 본다:

```bash
# V1 [차량 PC]
python3 ~/FMA_ws/src/stack_gps/tools/base_station/rtcm_server.py \
    --port /dev/ttyRadio --tcp-port 2101

# V2
ros2 launch adas_mgm REAL_VEHICLE_lane_gps_can.launch.py \
    REAL_VEHICLE_CONFIRM:=I_UNDERSTAND_THIS_ENABLES_REAL_CAN_TX \
    waypoint_csv:=$HOME/FMA_ws/src/stack_gps/waypoints/waypoints_straight_1_20260811_193556.csv \
    usb_speed:=high camera_fps:=10
    
# M (내내 켜 둠)
ros2 run adas_mgm state

# V3 — 점검 5종(avoid 포함) 통과 시 출발
ros2 run adas_mgm go
```

> **`usb_speed:=high camera_fps:=10`은 GPS를 위한 필수 인자다 (2026-08-14).**
> 빼면 OAK-D가 USB3(SuperSpeed)로 열거되고 그 방사 잡음이 GNSS L1을 덮어
> **RTK FIXED가 아예 안 잡힌다** (같은 위치에서 C/N0 39dB↔22dB). USB2는 대역폭이
> ~40MB/s라 `camera_fps:=10`을 반드시 동반해야 한다 (CLAUDE.md §6).
> 기동 후 `stack_lane` 콘솔에 `USB 링크 속도 제한: HIGH` 경고가 떠야 적용된 것이다.
>
> **⚠ `gps_only:=true`를 붙이면 차선 스테이트가 절대 안 뜬다 (2026-08-15 실측).**
> 이 인자는 `lane_conf_exit`·`lane_conf_return`을 **둘 다 2.0**으로 덮어쓰는데
> 신뢰도는 최대 1.0이라 LANE 전이가 **구조적으로 불가능**해진다 (덤프 헤더의
> `conf_exit=2 conf_ret=2`로 확인 가능). run_0815_162102에서 `lane_path` 1562개
> 수신·신뢰도 최대 0.79로 **인지는 정상이었는데** 170초 내내 gps였다 —
> 인지 고장으로 오진하기 쉬우니 주의. 변수를 줄이고 싶을 때만 §3-1로.

**기대 전이 로그 (M 터미널):**

```
[hh:mm:ss]   gps    (v_ref 0.60)      ← 출발 직후 (차선 신뢰도 히스테리시스 충족 전)
[hh:mm:ss] → 차선   (v_ref 0.60)      ← lane_conf > 0.70 이 50틱 연속 AND 트랙 재합류
[hh:mm:ss] → 회피   (v_ref 0.60)      ← 콘 앞 ~3m (회피 중 감속 금지 — CLAUDE.md §3)
[hh:mm:ss] → gps    (v_ref 0.60)      ← 감지 소실 6초 내외 뒤 (maneuver_done) — 차선 아님
[hh:mm:ss] → 차선   (v_ref 0.60)      ← 복귀 3초 이상 뒤 + 트랙 재합류 후
[hh:mm:ss]   gps    (v_ref 0.00)      ← 트랙 종점
```

> **종점 정지는 lane·waypoint 공통이다 (2026-08-15 수정).** 구현이 waypoint 전용이던
> 시절 run_0815_163614에서 종점 인지 후 **7.41m를 더 달렸다**(13.3초 지연, 그 사이
> 횡오차 0.41→6.37m + 트랙 밖 AVOID 진입). 지금은 스테이트와 무관하게 래치가 걸린다
> — 같은 덤프 재생 시 지연 13.34s→**0.38s**, 초과주행 7.41m→**0.17m**.
> 재발하면(종점을 지나쳐도 안 섬) **재빌드 여부부터** 확인할 것.

복귀 후 lane으로 넘어가려면 **3가지가 모두** 성립해야 한다 (2026-08-14):
① 최소 3초 경과(`avoid_return_hold_cycles`) ② 차선 신뢰도 히스테리시스
③ **트랙에 실제 재합류** — 횡오차 ≤ `lane_entry_max_cross_m`(0.5m).

③ 때문에 **이탈이 안 잡히면 lane으로 안 넘어가고 gps에 계속 머문다** — 그게
정상이다(이탈한 채 카메라로 넘어가면 트랙 복귀를 영영 못 한다). M 터미널에서
`gps`가 오래 유지되면 실패가 아니라 재합류 중인 것이니, `lateral.csv`의
`cross_track_m`이 0.5m 아래로 내려오는지를 보라. 안 내려오면 재합류 자체가
안 되는 것이므로 §5의 "복귀 후 재합류 실패" 항목으로.

**즉시 중단 기준 (V2 Ctrl-C 또는 물리 비상정지):**
- 콘 옆을 지나기 **전에** `→ gps` 복귀 (조기 done — 콘을 향해 재수렴한다)
- AVOID에서 콘 반대쪽이 아닌 **콘 쪽으로 조향**
- 이격이 눈짐작 0.3m 미만으로 스침

## 3. 변형 2가지

1. **변수 축소 (gps_only)** — 차선 전이를 빼고 **avoid↔waypoint만** 보고 싶을 때
   (야간 차선 오검출 등으로 §2가 재현이 안 될 때). **`usb_speed`·`camera_fps`는 유지**:

   ```bash
   ros2 launch adas_mgm REAL_VEHICLE_lane_gps_can.launch.py \
       REAL_VEHICLE_CONFIRM:=I_UNDERSTAND_THIS_ENABLES_REAL_CAN_TX \
       waypoint_csv:=$HOME/FMA_ws/src/stack_gps/waypoints/waypoints_straight_1_20260811_193556.csv \
       gps_only:=true usb_speed:=high camera_fps:=10
   ```

   이 모드에서는 **차선 스테이트가 절대 안 뜬다**(§2의 ⚠ 참조) — 정상이다.
   `stack_lane`은 그대로 떠서 사후 분석용 데이터를 남긴다.
   **카메라를 아예 빼려면** `gps_only` 대신 `lane_enabled:=false` + `ros2 run adas_mgm go --skip-lane`.
   ⚠ 사후 비교 시 **모드가 다른 run끼리 오실레이션을 비교하지 말 것** — 차선 스테이트가
   GPS보다 헤딩 진동이 크다(실측: LANE 헤딩 표준편차 9.2~9.7° ↔ GPS 3.4~3.8°).
   속도·파라미터 효과를 보려면 **같은 모드·같은 스테이트 구간**끼리 비교한다 (2026-08-15).
2. **열림 없음(narrow)**: 콘 3개로 양옆을 막고 접근 → narrow_gap 감속(v_narrow 0.2) →
   estop 정지 확인. 회피 시도 없이 감속·정지해야 정상.

## 4. 합격 판정

| # | 항목 | 기준 |
|---|---|---|
| 1 | AVOID 진입 | 콘 앞 2.5~3.5m에서 전이, 조향 방향 = 열린 쪽 |
| 2 | 통과 이격 | 0.4m 이상 (목표 0.55m) |
| 3 | 복귀 타이밍 | 콘을 **차 뒤로 완전히 보낸 뒤** 복귀 — 조기 done 없음. 복귀 직후 콘이 다시 감지되면(ttc<1) **측면 충돌 직전**이므로 즉시 중단 |
| 4 | 복귀 궤적 | GPS 트랙으로 부드럽게 재합류 (블렌드 — 급조향 없음) |
| 5 | 종점 정지 | 회피와 무관하게 정상 작동 |
| 6 | **복귀 후 gps 체류** | **3초 이상** — 한 틱 만에 lane으로 튀면 불합격 (2026-08-14 수정 확인) |
| 7 | **RTK 유지** | run 내내 `fix_quality=4` — `lateral.csv`의 quality 열로 사후 확인 |

7개 전부 합격 → 확정 파라미터를 `stack_avoid/config/params.yaml`에 반영 + 커밋/PR.

## 5. 증상 → 튜닝 노브 (주행 중 터미널에서, 재시작 불필요)

| 증상 | 원인 후보 | 노브 (`ros2 param set /stack_avoid_node ...`) |
|---|---|---|
| **AVOID 진입은 되는데 조향 없이 직진 → estop** | run_0812_234253에서 실측 — ref 1점 + v_ref 0.44(조향 하한 0.5 미달, 이기돈 검증)의 중첩 | **수정 완료** (① 조립 20점 보간 ② avoid 중 감속 제거 — CLAUDE.md §3 재개정). 재발 시 AVOID 중 `ros2 topic echo /adas/target_ref --once`로 ref_points=20 & v_ref≥0.5 확인 |
| **막힌 쪽으로 조향** (열린 쪽 반대) | 라이다 드라이버 reversion/inverted 불일치 → 스캔 좌우 거울상 (run_0813_001140에서 규명 — 전방·거리·estop은 정상이라 회피에서만 드러남) | **수정 완료** (통합 launch가 false/false 고정). 드라이버 params를 바꿀 땐 이 두 값 유지 필수 |
| 콘 사이 빈틈을 목표로 찍음 | 통과 최소폭(차폭+2×여유=0.92m) 이상인 틈은 설계상 유효한 통로 + depth_band(0.6m) 밖 콘은 못 봄 | 틈을 막거나(콘 밀착·판자), `avoid.lateral_margin_m`↑로 최소폭 상향 (0.25 → 1.12m), `avoid.depth_band_m`↑ |
| AVOID 진입 안 함 | 콘이 통로 밖 | 콘을 경로 중앙으로 (±0.46m) — 노브보다 먼저 |
| 〃 | 감지 거리 부족 | `avoid.detect_range_m` 3.0→4.0 |
| 〃 (avoidable=false) | ttc<1.5 | `avoid.ttc_stop_s` 확인 — 낮추는 건 신중히 |
| **조기 done → 복귀 중 측면 충돌** | 클리어런스 부족. 감지 소실은 통과가 아니라 조향으로 장애물이 통로를 측방 이탈한 것 (2026-08-14 실차 2회) | `avoid.clear_gap_max_m` 확인(3.0). 더 필요하면 `avoid.clear_margin_m` 0.3→0.8 |
| 복귀가 너무 늦음 | 클리어런스 과대 | `avoid.clear_gap_max_m` ↓ (3.0 기본). 단 **낮추면 조기 복귀=측면 충돌 위험** — 2026-08-14에 1.0이었다가 실차 2회 스침 |
| **통과 대기 중 조향 표류 → 복귀 후 발산/유턴** | 통과 단계 빈 경로 → MGM이 직전 목표 감쇠 hold → 퇴화 ref (run_0813_003037: 이탈 3.9m 루프 후 역방향 가드 정지) | **수정 완료** — 통과 단계에 전방 직진 유지점 (1.5, 0) 발행 |
| 이격 부족하게 스침 | 편측 여유 부족 | `avoid.lateral_margin_m` 0.15→0.25 (이격 +0.1m) |
| 열림 있는데 narrow 정지 | 오프셋 상한 | `avoid.offset_max_m` 1.0→1.3 |
| 목표점이 프레임마다 튐 | rate limit | `avoid.target_rate_limit_mps` 3.0→2.0 |
| AVOID 중 갑자기 정지 + "avoid 신선도 초과" 로그 | stack_avoid 사망 (watchdog 정상 동작) | V2 재시작 |
| **RTK FIXED가 아예 안 잡힘 + `lateral.csv`가 아예 없음** | `stack_gps_node`가 기동 실패로 즉사 — waypoint_csv 경로 오타나 1~4점짜리 잔여 CSV. launch는 나머지 노드를 그대로 띄우고 계속 돌아 **카메라만 보고 주행**하게 된다 (2026-08-15 3연속 실사례) | **수정 완료** — launch가 CSV를 미리 읽어 검증하고 거부한다. 그래도 의심되면 `ros2 node list \| grep stack_gps` (안 나오면 죽은 것) |
| **RTK FIXED가 아예 안 잡힘 (DGPS/FLOAT 고착)** | 카메라 USB3 방사 잡음 — 위성 수·HDOP·RTCM은 정상값 그대로라 상태줄로는 안 보인다 (2026-08-14 규명) | `usb_speed:=high camera_fps:=10` 인자 확인. 그래도 안 되면 launch 끄고 `python3 ~/FMA_ws/src/stack_gps/tools/rtk_probe.py --seconds 120` 로 **C/N0** 측정 — 38dB 미만이면 안테나를 하늘 트인 쪽으로 |
| **avoid 복귀가 한 틱 만에 lane으로** | 히스테리시스 카운터 미리셋 (2026-08-14 수정) | 재빌드 여부 확인. 체류를 더 원하면 `avoid_return_hold_cycles`↑ (MGM 파라미터, 300틱=3s) |
| **복귀 후 재합류 실패 — cross가 안 줄고 lane으로도 안 넘어감** | 게이트는 정상 동작(이탈 중엔 lane 금지). 문제는 재합류 자체. **원인 규명 완료 (2026-08-15, run_0815 4런)**: 재합류 목표점 거리가 이탈과 함께 커져(1.3m→5.4m) 조향 응답이 붕괴했다 — 목표 2.5m 초과 시 명령 곡률의 10%만 실현(CLAUDE.md §3 제약 ③). 이탈이 클수록 목표가 멀어지는 **양의 되먹임**이었다 | **수정 완료** — 재합류 목표를 1.27~1.8m로 클램프(`REJOIN_TARGET_MAX_M`) + avoid 20점 보간 첫 점을 1.2m로(`MGM_REF_FIRST_POINT_M`). 재발 시 AVOID·복귀 중 `ros2 topic echo /adas/target_ref --once`로 **`ref_points[0]`의 거리 `hypot(x,y)`가 0.8~1.8m인지** 확인(x>0은 기본). 밴드 밖이면 재빌드 여부 확인 |
| **AVOID 중 헤딩이 한쪽으로 흐름 (복귀 시 cross 2m+)** | AVOID의 "전방 직진 유지"는 **헤딩 개루프**라 dSPACE 잔류 조향(`str`≈0.05)이 그대로 적분된다 — 실측 1~2°/s. run_0815_144142는 12초 AVOID에 헤딩 +26.7°·cross 0.08→1.88m, run_0815_142817은 14.8초에 −10.3°(방향이 반대 = 계통 오차 아닌 표류) | `avoid_max_cycles`↓ (기본 1200틱=12s → 500틱=5s 권장). 개루프 시간이 곧 이탈량이다. 단 조기 복귀는 측면 충돌 위험과 교환 — §4 표의 클리어런스와 함께 볼 것 |
| lane↔gps 전이가 잦음 | 차선 confidence가 임계 밴드 안에서 진동 (평균 0.47 실측) | `lane_conf_return`↑ / `n_cycles`↑. 확정 전 `core_replay`로 기존 run을 재생해 비교할 것 |
| **차선 주행 중 좌우 위빙 (주기 20~30초, 헤딩 ±10°)** | GPS 구간에는 없고 **LANE에서만** 나타나며 긴 구간일수록 커진다(실측: 5.5s 구간 3.67° ↔ 72s 구간 9.22°, 같은 run의 GPS는 6.07°). **차선 추종 루프가 이득 과다다** — ref[0]을 당기면(=이득↑) 되레 커진다: 2.08m → `|str|` 0.0566·헤딩 9.22° / 1.88m → `|str|` 0.0875·헤딩 **10.83°**. 지연은 0.44→0.27s로 줄었는데도 나빠졌으니 지연이 아니라 이득이 지배한다. v_base 0.5→0.6으로 실현율이 14→24%로 오른 것(§3 ①)도 같은 방향이다 | **이득을 낮추는 쪽으로** 간다 — `ref_point0_lookahead_m` 1.8 → **0.0**(외삽 끔 = ref[0]이 `points_x_start` 2.5m로 균일). 미리보기 = L/v 로 보면 1.8m@0.5는 3.6s인데 0.6에서 같은 3.6s는 2.16m다 — **속도를 올리면 lookahead도 같이 나가야 한다.** 부족하면 `points_x_start`↑. ⚠ `ref_point0_min_confidence`(0.30)는 외삽을 켤 때만 의미가 있다. **한 번에 하나씩** 바꿀 것 |

| **GPS 구간만 좌우 오실레이션 (차선은 멀쩡)** | **재합류 기하 3종이 "조향이 느리던 시절"의 보상값이다** — dSPACE 조향 PI 도입(2026-08-17) 후 그 보상이 그대로 과이득이 됐다. run_0817_011045 같은 run 안에서 gps ↔ 차선: ref[0] 1.29↔2.06m · 명령 \|κ\| 0.212↔0.068 1/m · 방위 부호반전 1.53↔0.41회/s · 틱간 Δ방위 sd 6.41↔0.94° · 실제 요레이트 MAD 3.0↔1.0°/s. 주범은 ψₑ 변화율 감쇠항(`REJOIN_RATE_DAMP_S` 0.6) — 10Hz 헤딩을 미분해 0.6을 곱하니 잡음 이득 ×3이고, 실측 ψₑ sd 7.4°에 대해 **감쇠항 sd 13.6°·스파이크 296°** 로 댐핑 대상보다 항이 크다 | **①이 기본값으로 반영됨** (`rejoin_rate_damp_s` 0.6→0.0). 남으면 아래를 **한 번에 하나씩**, `ros2 param set /stack_gps_node ...` (⚠ stack_avoid 아님):<br>② `rejoin_full_cross_m` 0.5→1.0 (접근각 이득 절반)<br>③ `ref_lookahead_m` 1.0→1.8 **AND** `rejoin_target_max_m` 1.8→2.5 (둘 다 올려야 는다 — 거리는 `min(트랙점거리, 상한)`이라 한쪽만 올리면 안 움직인다) |

⚠ `param set`으로 바꾼 값은 **재시작하면 사라진다** — 확정값은 params.yaml(또는 launch 기본값)에 반영해야 다음 세션에 살아남는다.

### 5-1. 재합류 기하 A/B (2026-08-17, 실주행 fix 재생 — 사후 검증됨)

`drive_logs/run_0817_*`의 실제 fix를 `PathEngine`에 그대로 먹여 재생한 결과다
(로그 `cross_track_m`과 재계산값이 0.2cm 차 → 웨이포인트 CSV 일치 확인).
**gps 스테이트 구간만** 집계, 두 run 일관:

| 설정 | Δ방위 sd | 부호반전 | ref[0] | 명령 \|κ\| |
|---|---|---|---|---|
| 이전 (damp 0.6) | 4.34° | 1.94회/s | 1.28m | 0.123 |
| **① damp 0.0 ← 새 기본값** | **2.47°** | **0.75회/s** | 1.28m | 0.141 |
| ② + full_cross 1.0 | 1.92° | 0.67회/s | 1.28m | 0.119 |
| ③ + lookahead 1.8 / max 2.5 | 1.96° | 0.58회/s | 1.97m | 0.079 |
| (참고: 같은 run의 **차선** 구간) | 0.94° | 0.41회/s | 2.06m | 0.068 |

①은 **추종 이득을 건드리지 않고 채터링만** 없앤다(\|κ\| 유지) — 그래서 1단계다.
③까지 가면 GPS 기하가 차선 수준이 되지만, 목표 거리를 늘리는 것은 조향 실현율
곡선(CLAUDE.md §3 ③)에 걸리므로 **아래 재측정 전에는 ②까지만** 권장한다.

### 5-1b. 2026-08-17 dSPACE 병합 분석 이후 (run_0817_020112)

dSPACE 로그를 counter로 정합(21300/21300, 100.00%)해 원인을 갈랐다. **잡음 자체는
GPS가 더 작다** — 갱신당 Δψₑ 0.89°(차선 0.72°), Δα 0.93°(차선 1.15°). 그런데 결과는
Δ기하δ 1.51° ↔ 차선 0.39° 로 3.9배다. 증폭은 세 군데였고 셋 다 손봤다:

| 원인 | 조치 | 기본값 |
|---|---|---|
| `cross_track_m` 이 트랙 **선**이 아니라 최근접 **꼭짓점**까지 거리 → 웨이포인트 간격이 만드는 주기 0.583s·진폭 0.081m 가짜 톱니 (이론 0.083m 과 2mm 이내 일치) | 선분 투영 수직거리로 교체 (`_foot_on_track`) | (수정 반영) |
| 접근각 α 가 쓰는 e 에 종방향 성분이 누설 (≈ 종방향 offset × sin ψₑ, 실측 0.016m → α 0.82°) | e 를 **수선의 발**로 계산 → 누설 0 | (수정 반영) |
| ref[0] 거리의 **81.7% 가 기하 하한 1.267m 에 고착** → κ=2sin(b)/d 라 차선(2.04m) 대비 1.61배 증폭 | 하한을 파라미터화 + 밴드 [1.27,1.8] 의 완만한 끝으로 | `rejoin_target_min_m` **1.8** |
| Δα 의 54% 가 백색잡음 (sd 0.628° = e 환산 1.26cm, RTK 위치 잡음) | e 에만 1차 저역통과. **ψₑ 에는 걸지 말 것** — Δψₑ 의 lag-1 자기상관이 +0.002 로 백색 성분이 0이라 필터가 실동작만 죽인다 | `rejoin_e_lpf_s` **0.15** |

같은 run 의 fix 열을 새 엔진에 재생한 결과 (개루프 — 차는 다르게 굴렀을 것이므로
**잡음 비교용**):

| 설정 | \|Δcross\| | \|Δ기하δ\| | p95 | 부호반전 |
|---|---|---|---|---|
| 종전 (재현) | 3.42 cm | 1.41° | 4.75° | 0.98/s |
| ① 수직거리 | **1.36 cm** | 1.29° | 4.27° | 0.83/s |
| ①+② e LPF 0.15s | 1.36 cm | 1.05° | 3.91° | 0.78/s |
| ①+②+③ 하한 1.8m (신규 기본) | 1.36 cm | **0.77°** | **2.99°** | 0.78/s |
| (차선 실측) | — | 0.39° | 1.08° | 0.33/s |

재현 행(1.41°/4.75°)이 실제 폐루프 로그(1.51°/4.97°)와 맞으므로 재생 하네스는 신뢰할 만하다.

**실차 시험 순서** — 한 번에 하나씩, 되돌리려면 파라미터만 원복:
1. 수직거리 수정만 (파라미터 없음, 코드 반영) — `lateral.csv` 의 `cross_track_m` 이
   웨이포인트 통과 주기로 톱니치지 **않는지** 먼저 확인. 여기서 §4 전이 게이트도 같이 정확해진다.
2. `ros2 param set /stack_gps_node rejoin_e_lpf_s 0.0` ↔ `0.15` 비교.
3. `rejoin_target_min_m` 1.267 ↔ 1.8 비교. ⚠ 이것만은 **§5-2 재측정과 같은 성격**이다 —
   조향 실현율이 정말 회복됐다면 완만한 쪽이 이기고, 아니면 복귀가 굼떠진다.

### 5-1c. ✖ v_base 1.0 m/s 시도 → **되돌림** (2026-08-17, run_0817_041219)

속도(0.6→1.0) + 안전값(estop 0.70/0.80→1.20/1.35, ttc_stop 0.8→1.3) + 회피
(detect_range·clear_gap_max·target_rate_limit 3.0→5.0) 을 세트로 올려 79초 주행했다.
**AVOID 가 79초 중 41초(52%)를 먹고 횡오차가 2.08m 까지 벌어져 되돌렸다.**

| | 0.6 m/s (031729) | 1.0 m/s (041219) |
|---|---|---|
| obstacle_detected | 4% | **28%** |
| AVOID 진입 / 체류 | 1회 / 12.0s (13%) | **4회 / 40.8s (52%)** |
| narrow_gap | 0% | 5% |
| AVOID 중 v_ref | 0.60 고정 | 평균 0.87 · **최소 0.05 · 15%가 0.5 미만** |
| 횡오차 최대 | 0.53m | **2.08m** |
| 전이 | 3회 / 93s | **11회 / 79s** |

**차선 구간은 1.0 m/s 에서도 멀쩡했다** — cross 평균 0.061~0.114m, Δ기하δ 0.079~0.089°
(0.6 m/s 와 동등). 횡방향 분석은 맞았다. 무너진 건 회피다.

**내가 틀린 지점 — `detect_range_m` 3.0 → 5.0.**
"경고 시간 복원 + estop 이 뺏은 가용거리 복구" 라는 근거 자체는 맞지만, **AVOID 가
한 번 걸리면 12초를 강제로 머문다**는 것을 계산에 안 넣었다. 아래 실측대로
`maneuver_done` 이 사실상 안 뜨기 때문에 매 진입이 `avoid_max_cycles` 상한(1200틱
= 12.0s)까지 간다. 그래서 **중요한 건 감지 거리가 아니라 감지 빈도**다 —
진입 1회당 12초·1.0 m/s 면 12m 를 회피 경로로 달린다. 실제로 회랑 점유 자체는
두 run 이 거의 같았다(3.0m 이내 14.0% ↔ 15.4%). 거리를 늘린 만큼 트리거가 늘고,
트리거 하나가 12초를 먹는 구조라 52% 가 나왔다.

**★ 그 과정에서 드러난 선행 결함 (이기돈 — 속도와 무관, 0.6 m/s 에도 있다):**
`maneuver_done` 이 거의 안 뜬다. 0.6 m/s run 의 유일한 AVOID 도 12초 상한으로
강제 종료됐고, 1.0 m/s 의 4회 중 3회가 그랬다(총 maneuver_done 0.6s / 1.2s).
즉 **회피 복귀가 정상 경로가 아니라 안티고착 상한으로 이뤄지고 있다.**
1.0 m/s 재시도 전에 이것부터 고쳐야 한다 — 안 고치면 속도만 올라가 이탈 거리가
비례해서 커진다.

**정지거리 실측은 살려 둘 것** (이 값들은 유효하다):
```
v0 0.591 m/s → 반응지연 0.13s → 감속 0.94 m/s² → 0.76s / 0.314m 에 정지
감지 지연 /scan 9.9Hz × 확정 3프레임 = 0.30s
필요거리 = 0.303·v + 1.19·(0.13·v + v²/(2·0.94))
  0.6 → 0.49m | 0.8 → 0.77m | 1.0 → 1.09m   (estop_on 0.70m 은 0.6 m/s 전용)
```
→ **속도를 올리려면 estop 문턱을 반드시 같이 올려야 한다**는 결론은 유효하다.
다만 문턱을 올리면 회피 가용거리가 줄어드는 맞교환이 있고, 그 보상으로
detect_range 를 늘리는 건 위 이유로 **틀린 처방**이다.

**재시도 순서** (`maneuver_done` 수정 후):
1. 0.6 m/s 에서 `maneuver_done` 이 정상적으로 뜨는지 먼저 확인 (12초 상한에 안 걸리는지).
2. estop 문턱만 1.20/1.35 로 올리고 0.6 m/s 유지 — 회피가 여전히 성립하는지.
3. 그 다음 0.8 m/s. detect_range 는 **건드리지 말 것**.

### 5-2. ⚠ 조향 PI 이후 **실현율 곡선 재측정**이 최우선

**2026-08-17 부분 진척 (run_0817_020112, dSPACE 병합):** 체인을 두 단으로 갈라 재면
`실제δ/명령δ`(서보 PI 루프) = 차선 79.9% · GPS 91.7% · 회피 107.4%, 지연 160~180ms.
**서보는 회복됐다.** 남은 손실은 `명령δ/PC 기하δ`(MPC) = 43~59% 쪽이다.
그리고 그 43~59% 조차 **우리 잡음에 깎인 값**이다 — 저역통과 폭을 늘리면 차선은
49%로 평평한데(진짜 플랜트 이득) GPS만 54→70%로 오른다 = GPS 명령의 1/4은 차가
따라갈 수 없는 고주파다. 여전히 **거리 밴드별** 곡선은 안 재봤으므로 아래는 유효하다.

CLAUDE.md §3 ③의 "명령 곡률의 10~55%만 실현 / 2.5m 넘으면 10% 아래로 붕괴"는
**PI 이전 측정**이다. 이 법칙 하나에 세 가지가 매달려 있다:
GPS 재합류 기하 3종 · avoid 20점 보간의 첫 점(목표의 1/20 ≈ 7.5cm, `mgm_step.cpp`) ·
"보상 기하를 정직하게 고치지 말 것"이라는 §3 ③ 경고 자체.

측정 방법(장애물 없이 직선 코스 1런이면 된다):
목표 거리를 밴드별로 바꿔 가며(`ref_lookahead_m` + `rejoin_target_max_m`) 주행하고,
명령 곡률 κ_cmd = 2·sin(b)/d 대비 **실현 곡률 = 요레이트 ÷ v** 를 비교한다.
`/vehicle/vector`가 안 오므로 `str` 대신 `lateral.csv`의 heading 미분을 쓴다
(`heading_src == 융합` 구간만 — 접선 폴백은 헤딩이 점프한다).
곡선이 바뀐 것이 확인되면 그때 CLAUDE.md §3 ③과 avoid 보간 기하를 함께 개정한다.

## 6. 시험 후

- rosbag·스냅샷은 `~/FMA_ws/drive_logs/run_<시각>/`에 자동 — Claude에게 "회피 run 분석"을
  요청하면 진입 시점·회피 기하·done 타이밍·전이 연속성을 뽑아준다.
- **dSPACE 로그(다른 PC에서 측정)를 같이 봤다면** — CSV를 run 디렉터리에 복사한 뒤:

  ```bash
  python3 src/adas_mgm/tools/dspace_merge.py drive_logs/run_<시각> \
      --dspace drive_logs/run_<시각>/dspace_<시각>.csv
  ```

  두 PC는 시계가 달라 그냥 겹칠 수 없다 — 도구가 `0x100`의 **counter**(없으면 v_ref
  파형 상호상관)로 시각을 맞춘 뒤, PC의 스테이트 구간으로 dSPACE 파형을 색칠한다
  (어디가 GPS/차선/회피였는지 대조). **측정 변수는 시험 전에 잡아야 한다** —
  `src/bridge_dspace/DSPACE_LOGGING.md`의 표대로 (counter·state 포함).
- 확정 파라미터 → `stack_avoid/config/params.yaml` 반영 → 커밋/PR (이기돈 멘션).
- maneuver_done·v_suggest 계수가 초기값과 크게 다르면 `stack_avoid/stack_avoid/node.py`의
  해당 주석(★ 이기돈 검증 필요)도 실측값 근거로 갱신할 것.

## 참조

- 기본 주행 절차·RTK·문제 진단: `RUNBOOK_lane_gps.md`
- 회피 단독 시험·계수 배경: `src/stack_avoid/FIELD_TEST_PLAN.md`, `MEASUREMENTS.md`
- 스테이트 전이·우선권: CLAUDE.md §4 / 입력 watchdog: CLAUDE.md §5.7 ⑤
