# 무력화됨 — 99-fma-lidars.rules 가 대체한다 (2026-08-25)
#
# 원본은 vid/pid(10c4:ea60) 만 보고 매칭해서 라이다 4대와 **IMU 까지** 전부
# /dev/ydlidar 를 주장하게 만들었다. 실제 링크는 마지막에 처리된 장치를 가리켜
# 비결정적이었다. 이제 /dev/ydlidar 는 전방 T-mini 하나만 가리킨다.
#
# 설치: sudo cp 99-ydlidar.rules /etc/udev/rules.d/   (기존 파일을 덮어쓴다)
